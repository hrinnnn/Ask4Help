# 实验总计划

## 目标

补齐面向 robot-gated DAgger 的多阶段 OOD 与 takeover-timing 实验。每个 stage 都是正式 benchmark 条件，不从中挑选“最有效”的 stage。

## 子计划入口

- [训练与持续运行协议](训练与持续运行协议.md)
- [Stage-Localized OOD 任务设计规范](plans/Stage-Localized_OOD任务设计规范.md)
- [X-VLA StackCube 目标位置 OOD 与受控介入](plans/X-VLA_StackCube目标位置OOD与受控介入.md)
- [X-VLA StackPyramid 全阶段 OOD](plans/X-VLA_StackPyramid全阶段OOD.md)
- [Takeover Timing Sweep](plans/Takeover_Timing_Sweep.md)

## 执行顺序与认领

`认领`列是多 Agent 协作锁。格式为 `已认领｜<Agent/任务标识>｜<北京时间>｜<运行位置>`；确认无人负责时写 `未认领`。认领必须在启动实现或服务器进程前写入。任务完成、明确暂停或释放时及时更新；接管已有任务时写 `已接管`，并在对应子计划记录原任务和后台进程的核实结果。由于该机制从 2026-08-14 开始使用，历史条目暂记为 `未登记，认领前核实`，不能据此直接判断没有后台工作。

| 顺序 | 任务 | 状态 | 认领 | 完成条件 |
|---|---|---|---|---|
| 0 | 整理环境、共享协议、训练运行规则与历史结果 | 已完成 | 不适用 | 本目录核心文档互相一致 |
| 1 | [X-VLA StackCube 目标位置 OOD 与受控介入](plans/X-VLA_StackCube目标位置OOD与受控介入.md) | retry2 已完成：四组训练、共同 checkpoint selection、100-ID/100-OOD final evaluation 已落盘 | 已完成｜H20｜2026-08-14 | 四种 timing condition 的 matched-budget 训练与固定评测完成 |
| 2 | [X-VLA StackPyramid 全阶段 OOD](plans/X-VLA_StackPyramid全阶段OOD.md) | 部分完成：base policy、三个 Stage 的 Internal-Feature PCA 收集/训练/评测已完成；Diff-DAgger、Failure-Recovery、Offline BC 三个对照分支尚未对三个 Stage 全部完成，因此整项任务不得标记完成 | 已接管｜codex-stackpyramid-four-method-comparison｜2026-08-15｜5090 `111.198.58.150:12001` | Stage 1/2/3 各完成 PCA、Diff-DAgger、Failure-Recovery、Offline BC 的独立 OOD 收集、matched-budget 更新与共同 ID/OOD 评测 |
| 3 | [X-VLA StackPyramid timing sweep](plans/Takeover_Timing_Sweep.md) | 已完成第一轮 stage-localized Bridge-PCA 采集与 matched-budget 更新；完整多介入时机 sweep 仍需执行。原控制器与历史 PID 已核实退出，当前接管后推进 | 已接管｜codex-stackpyramid-timing-recovery｜2026-08-15｜5090 | 每个 stage 的多 timing condition 均完成受控收集、等专家动作预算更新与训练后 SR |
| 4 | [新任务与全阶段 OOD](plans/Stage-Localized_OOD任务设计规范.md) | UncoverSpherePlace：3x3 oracle gate、9/9 resume audit、三 split 正式采集与 temporal-mask 验收已完成；ID base policy 已完成 10000 步，retry2 对 2000/4000/6000/8000/10000 的独立 selection 均为 0/20 ID success，已写出 `BASE_POLICY_NOT_ACCEPTED`，下游阶段未启动 | 已认领｜codex-uncover-sphere-place｜2026-08-14｜5090 | 任务、启用的 stage-localized OOD、oracle、ID base policy 和正式评测完成 |
| 5 | [OpenDrawerRetrievePlace ID 与阶段 OOD](plans/OpenDrawerRetrievePlace_ID_and_stage_OOD.md) | 协议审计接管：旧 ID base 10/100 不合格；旧 quota collection、detector 与 matched-update 仅作 diagnostic；等待全新 ID base 验收 | 已接管｜codex-opendrawer-protocol-audit-v2｜2026-08-15｜5090 `111.198.58.150:12001` | 新 ID base 独立100-ID至少80%；四 split Oracle各至少100次且至少90%；随后三 split独立四方法流程完成 |
| 6 | 真机验证 | 暂缓 | 未登记，认领前核实 | 仿真结论与最终协议冻结后再设计 |

## 每个 stage 的固定步骤

1. 冻结 ID/OOD 条件、seeds、oracle 与 base checkpoint；确认 oracle 在 ID 和当前 OOD 上成功率均达到任务门槛，且 base policy 已掌握 ID、在当前 OOD 上存在受控缺口。
2. 跑纯 policy rollouts，完成 failure-detection comparison。
3. 对每个 gated 方法，从同一 base policy 运行冻结的 ID/当前 OOD 严格交替 deployment stream，直到自然得到 100 条成功且发生真实接管的 expert trajectories；不设 accepted-ID/accepted-OOD 配额。Internal-Feature PCA、Diff-DAgger、Failure-Recovery 分别形成独立且应显著 OOD-dominant 的数据；Offline BC 直接收集当前 stage 的完整 OOD oracle demonstrations。不得只完成其中一组。
4. 审计四份数据的动作、状态、视频、触发区间和 temporal-mask 边界，再按专家低层动作数选择共同预算。
5. 四组分别使用原始 ID + 本组 OOD expert 数据，从同一 base checkpoint 独立训练；不得让一组数据混入另一组。
6. 按《训练与持续运行协议》保存并评测共同训练节点；不得为不同方法分别挑选最佳 step。
7. 在相同固定 ID/OOD seeds 上评测四个更新后策略的 Success Rate；mixed ID/OOD stream 另用于评价实际数据选择。
8. 更新结果索引，逐组写明 collection、training、evaluation 路径、完成标记、成功定义和已知限制。

因此，一个 stage 的完成产物至少包括：`1` 个共同 base checkpoint、`4` 份 OOD expert datasets、
`4` 个独立更新后 policy 以及 `4` 组共同 ID/OOD evaluation。若任务含三个 OOD stages，则方法比较
部分原则上对应 `12` 份 stage-method 数据、`12` 个更新后 policy 和相应评测。受控四时机 sweep
另按《Takeover Timing Sweep》执行，不计入这四个方法分支。

Gated collection 的 `100` 是每个 stage-method 的 accepted 总量，不是每个 split 各 50 条。若
PCA/Diff/Failure-Recovery 的 accepted ID 接近或超过 OOD，该 stage 不得进入训练；先按《共享实验协议》
审计 detector 与采集器。训练公平性通过采集后的共同 expert-action budget 实现，不能通过改变采集分布实现。

Timing sweep 的条件、DCA/EAS/DCE 和启动门槛统一记录在对应子计划中，不在单个任务里另行修改。

## Agent 接力规则

1. 新 Agent 先按 `AGENTS.md` 顺序读完实验管理文档，再检查服务器上的实际完成标记。
2. 从总计划中选择第一个未完成且依赖已满足的任务。若显示 `未登记，认领前核实`，先核实没有现存负责人、总控或后台进程；若显示 `未认领`，仍需快速检查实际完成标记。确认后先写入认领信息，再开始任何实现或服务器操作。
3. 已认领任务不得重复执行。需要接管时先核实原 Agent、PID、总控状态、日志和完成标记，再把认领改为 `已接管`。
4. 复用共享协议；模型专属参数以该任务的脚本和 manifest 为准。
5. smoke 通过后由持久化总控程序自动执行同一任务内的收集、训练和评测，无需逐阶段重复询问；心跳只监督和恢复总控，不以单个阶段结束为完成。
6. 只有任务定义、指标定义或预算需要改变时才暂停请求确认；一般运行错误应先诊断和恢复。
7. 每完成一个阶段，立即更新本文件状态和《结果索引》，再继续下一阶段；整项任务结束或释放时同步清理认领信息。

若一个总计划条目包含多个可独立并行的子任务，先在对应子计划中拆出子任务认领表，再按最小独立单元认领；不得用一个宽泛的父任务认领阻塞所有并行工作，也不得在没有子任务登记时多人同时修改同一条 pipeline。
