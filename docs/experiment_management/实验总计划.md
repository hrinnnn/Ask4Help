# 实验总计划

## 目标

补齐面向 robot-gated DAgger 的多阶段 OOD 与 takeover-timing 实验。每个 stage 都是正式 benchmark 条件，不从中挑选“最有效”的 stage。

## 子计划入口

- [训练与持续运行协议](训练与持续运行协议.md)
- [Stage-Localized OOD 任务设计规范](plans/Stage-Localized_OOD任务设计规范.md)
- [X-VLA StackCube 目标位置 OOD 与受控介入](plans/X-VLA_StackCube目标位置OOD与受控介入.md)
- [X-VLA StackPyramid 全阶段 OOD](plans/X-VLA_StackPyramid全阶段OOD.md)
- [Takeover Timing Sweep](plans/Takeover_Timing_Sweep.md)

## 执行顺序

| 顺序 | 任务 | 状态 | 完成条件 |
|---|---|---|---|
| 0 | 整理环境、共享协议、训练运行规则与历史结果 | 已完成 | 本目录核心文档互相一致 |
| 1 | [X-VLA StackCube 目标位置 OOD 与受控介入](plans/X-VLA_StackCube目标位置OOD与受控介入.md) | 方案已冻结，待实施 | 四种 timing condition 的 matched-budget 训练与固定评测完成 |
| 2 | [X-VLA StackPyramid 全阶段 OOD](plans/X-VLA_StackPyramid全阶段OOD.md) | 总体方案已落地，待任务审计 | Stage 1/2/3 分别完成完整实验链路 |
| 3 | [X-VLA StackPyramid timing sweep](plans/Takeover_Timing_Sweep.md) | 共性协议已落地，等待任务 2 | 每个 stage 均完成受控 timing 与训练后 SR |
| 4 | [新任务与全阶段 OOD](plans/Stage-Localized_OOD任务设计规范.md) | 五个主候选与一个备用候选已登记，待逐项实现与 oracle 验收 | 任务、启用的 stage-localized OOD、oracle、ID base policy 和正式评测完成 |
| 5 | [OpenDrawerRetrievePlace ID 与阶段 OOD](plans/OpenDrawerRetrievePlace_ID_and_stage_OOD.md) | ID Oracle 数据与中间接管验收完成，待 base policy | 2000-step ID base policy、stage OOD 与 robot-gated 对比完成 |
| 6 | 真机验证 | 暂缓 | 仿真结论与最终协议冻结后再设计 |

## 每个 stage 的固定步骤

1. 冻结 ID/OOD 条件、seeds、oracle 与 base checkpoint。
2. 跑纯 policy rollouts，完成 failure-detection comparison。
3. 按同一部署流收集 PCA、Diff-DAgger、Failure-Recovery 与 Offline BC 数据。
4. 按专家低层动作数匹配预算，从同一 base checkpoint 更新策略。
5. 按《训练与持续运行协议》保存并评测共同训练节点；不得为不同方法分别挑选最佳 step。
6. 在固定 OOD seeds 上评测 Success Rate；mixed ID/OOD stream 另用于评价实际数据选择。
7. 更新结果索引，写明路径、完成标记、成功定义和已知限制。

Timing sweep 的条件、DCA/EAS/DCE 和启动门槛统一记录在对应子计划中，不在单个任务里另行修改。

## Agent 接力规则

1. 新 Agent 先按 `AGENTS.md` 顺序读完实验管理文档，再检查服务器上的实际完成标记。
2. 从总计划中选择第一个未完成且依赖已满足的任务。
3. 复用共享协议；模型专属参数以该任务的脚本和 manifest 为准。
4. smoke 通过后由持久化总控程序自动执行同一任务内的收集、训练和评测，无需逐阶段重复询问；心跳只监督和恢复总控，不以单个阶段结束为完成。
5. 只有任务定义、指标定义或预算需要改变时才暂停请求确认；一般运行错误应先诊断和恢复。
6. 每完成一个阶段，立即更新本文件状态和《结果索引》，再继续下一阶段。
