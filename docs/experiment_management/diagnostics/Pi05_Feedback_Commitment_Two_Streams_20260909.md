# Timing Feedback候选：两组新seed结果

这是对原v1零结果的探索性修订，不覆盖原方法、原seed或原报告。来源为H20：
`/mnt/data/ask4help/results/pi05_timing_feedback_ablation_v1/commitment_candidate_v1/airplane/stream_{1772000,1773000}`。
两组都完成fixed/feedback各20次真实rollout及独立raw-array/chronology审计；PCA、初始阈值及其他kernel参数不变，新线索阈值仅取ID校准和既有10mm尺度下限。

| 条件 | OOD双方均报警时：提前/相同/推迟 | 接管变化中位数（动作步） | OOD未报警 off→on | 共同完整后缀预算 | TASR×3 off→on | 实际专家动作总成本 off→on |
|---|---:|---:|---:|---:|---:|---:|
| 新stream1772000 | 6/2/0 | −35 | 2/10→0/10 | 1327 | 200/1327→345/1327（15.07%→26.00%） | 2247→2497 |
| 新stream1773000 | 5/1/0 | −15 | 4/10→3/10 | 167 | 0/167→0/167；筛选后没有OOD后缀 | 1292→1430 |

时间变化的分母只包含双方都报警的8/6条，未报警单列，不能把它们当作晚报警时间填充。每条比较截至首次接管之前的动作完全匹配。

第一组在×1/×2/×3下分别为10.10→16.65%、11.98→22.31%、15.07→26.00%。同时ID也有6/6条配对报警提前，实际专家成本增加；不能只报道收益。

第二组成功后缀仅off2条/on5条，最大共同可达整后缀预算退化为两组的同一条ID后缀。这个结果在数值上完整，但不能检验OOD的数据价值，不能叫“复现TASR提升”。保留全部已接受后缀时，描述性TASR×3为47/337=13.95%与181/900=20.11%；两组数据量不同，此项只能辅助说明，不能替代等预算主比较。下一步需要扩大采集，而不是按效果改预算规则。

## 当前能说什么

新增线索在两个独立小stream里都确实改变了后续真实接管时间；第一个stream的等保留预算数据价值出现正向信号。还不能认定稳定提高最终学习效果。Plane主要任务终点ever_grasped在两个stream中均未因这些辅助接管变化而增加；辅助strict成功也不是后SFT的无辅助SR。

原v1必须一起保留：SC matched296的TASR×3为122→123个兼容点；原Plane matched828为104→104、全部接管时间相同。没有已完成的新SFT或正式SR。

## OpenDrawer推进

原始128条ID示范及22973 anchors已完整恢复。同一ID参考在H20 native批量1运行时重算，保持rank1000，避免跨服务器cached Bridge差异进入阈值校准。已有controller执行完整参考→32-demo/50-ID-policy校准→独立审计。

Stage2 Grasp-OOD与Stage3 Goal-OOD可共用同checkpoint/norm/成功ID校准，但各自重置反馈记忆、分开采集和报告。Stage3必须检查策略是否到达抓起之后；若失败发生于前置阶段，只报告分阶段漏斗，不能声称验证了放置阶段的接管优势。
