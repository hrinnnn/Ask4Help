# Ask4Help 项目总控与研究记录

更新时间：2026-08-17
负责人：Codex 项目总控

## 1. 项目主线

当前论文的核心不是“再跑几个成功率”，而是回答一个更具体的问题：

> Robot-gated DAgger 需要在两个层面做出正确的数据采集决策：先决定哪些 rollout 值得请求专家，再决定在 rollout 的哪个状态开始接管。接管时机决定了专家数据是冗余的完整示范、直接完成任务的短 suffix，还是失败后的恢复轨迹。

因此，所有正式实验都要围绕以下链路组织：

`可靠任务与 base policy -> data selection -> takeover timing -> expert data -> matched-budget policy update -> ID/OOD evaluation`

Internal-feature PCA 是一个可部署的 gate 和 timing proxy，但它不能替代任务设计、时机对照或下游学习验证。论文中应把 PCA 的结果放在这条因果链上解释，而不是把单独的 detector AUPRC 当成整项工作的结论。

## 2. 当前总状态

| 任务 | 当前状态 | 已确认事实 | 下一道门 | 正式结果状态 |
|---|---|---|---|---|
| StackPyramid timing | Oracle Repair canonical formal ID gate failed scientifically | Repaired Oracle/data audits passed; fresh training from canonical X-VLA completed. The canonical retry4 formal gate has `100/100` episodes, videos, action arrays and state timelines, geometry=v4, fresh lifecycle, `formal_evidence_errors=[]`, strict success `0/100`, ever-grasped `82/100`, red-lifted `41/100`, red-placed `0/100` | Preserve all probe/formal diagnostics; do not enter prefix/locality, OOD, PCA or timing. Any future recovery must be a new, pre-registered, scientifically distinct base/data plan | Formal ID gate failed; downstream locked |
| StackPyramid 四方法 | 阻塞：唯一 v4 ID base 未通过 gate | 不得自行维护几何；不得使用 v3 或候选 v4；未开始正式采集/训练 | 等待新的、经独立 ID gate 验收的 immutable base；不得用当前 `ckpt-10000` 解锁 | 暂不可写入主结果 |
| OpenDrawerRetrievePlace | Checkpoint provenance audit: v3 collection/merge diagnostic preserved; 2-step smoke completed finite but controller expected the wrong experiment-name checkpoint path and wrote `PIPELINE_FAILED`; no continuation/OOD | No task-specific OpenDrawer X-VLA candidate found. Generic pi0.5 base, actual v3 smoke step2, old prompt-mismatch pi0.5 checkpoints, Airplane X-VLA and StackCube X-VLA are distinct candidates with different contracts | Confirm the exact checkpoint user means; only then prepare a verified checkpoint-writing smoke. Do not relabel generic/prompt-mismatch artifacts or launch training/OOD | A4H15/open_drawer_checkpoint_provenance_audit |
| StackCube Stage 2 PCA/Diff | 主流程已完成；Two-way gate retry3 score-separation audit 已完成并停止为 diagnostic | Diff raw `620`（ID/OOD 各310、严格交替），accepted `100`（ID10/OOD90），expert actions `2126`，collection audit PASS；PCA/Diff 各 `434` expert actions，独立训练与 2-step smoke PASS；共同 selection 选择 step2000；四个 final 分支各有 `100/100` 视频与 actions；`PIPELINE_COMPLETE` 已写入。Two-way retry3 已修正 canonical residual slice，20 条 smoke 有真实 takeover，但 `0/287` 专家决策低于 lower，return-to-policy 与二次 takeover 均为0 | 保留当前 Stage2 为 request-to-expert 证据；不调阈值、不跑正式100条、不修改 PCA/Diff 主结果 | Stage 2 complete; Two-way diagnostic complete |

### 当前结论

1. StackCube Stage 2 的新 PCA/Diff 分支已完成数据、训练、共同 checkpoint selection 和 held-out final evaluation；`PIPELINE_COMPLETE` 已核实。随后获准的 task-level two-way gate 已完成 20 条 smoke，但因没有触发真实 takeover 而按协议停止为 diagnostic。
2. StackPyramid 的正确动作是等待唯一 v4 owner 完成门禁，而不是让两个 Agent 各自“修到能跑”。
3. OpenDrawer v7 的最终 step10000 独立 ID gate 为 `34/100`，低于 `80/100`，因此旧 base 不合格；新 recovery 已通过 planner/Oracle smoke并进入同分布 ID collection，但尚未开始训练或 OOD。
4. v1/v2/v3 的 StackPyramid 数据、norm、checkpoint 和 detector asset 全部保留作诊断，但不得混入 canonical 结果。
5. v4 Oracle gate 已经是有效前置证据；当前阻塞点是 ID demonstration 的可靠落盘和完整验收，不是继续修改任务几何。
6. v4 ID demonstration 的存储问题已通过本地 staging 解决；当前 v4 base policy 在 ID 红块放置阶段失败，必须停在 diagnostic，不能用 OOD 或 gate 方法掩盖基础策略不合格。

### 2026-08-19：StackPyramid Oracle Repair canonical formal closure

- `formal_geometry_lifecycle_smoke_retry4` 已通过 3/3：每条 episode 使用独立 fresh environment，环境 ID 为 `Ask4HelpStackPyramidID-v4`，reset predicates 全部为 false，v4 red--green 初始距离均在冻结 `[0.14,0.18]` m 范围内。
- `formal_id_gate_100_retry4` 是唯一 canonical formal ID gate，使用 `ckpt-10000` 与冻结 seeds `84400--84499`。100/100 episodes、videos、actions、states、phase evidence 均完整，`formal_evidence_errors=[]`，几何为 v4，生命周期为 fresh-per-episode。结果为 `ever_grasped=82/100`、`red_lifted=41/100`、`red_placed=0/100`、`strict_success=0/100`，marker 为 `FORMAL_ID_GATE_FAILED` 与 `ID_BASE_NOT_ACCEPTED_FORMAL_100`。
- retry2 因实际使用 v1 geometry 标记 `FORMAL_ID_GATE_INVALID_GEOMETRY_MISMATCH`，retry3 因复用环境造成 reset contamination 标记 `FORMAL_GATE_FAILED_ENGINEERING_RESET_LIFECYCLE`；两者均保留为 diagnostic，不能与 retry4 合并或贡献分母。该结论表明正确 v4 ID 分布下策略仍未学会红块放置闭环，因此 prefix/locality、OOD、PCA、timing 和四方法流程全部锁定；本阶段没有存活的训练或评测进程。

### 2026-08-19：StackPyramid ID Recovery 512 认领

- 用户已批准继续同分布 ID recovery。XVLA Stack Hub 认领新 pipeline，运行位置为 H20 GPU0；GPU1 和无关进程保持不动，OOD/PCA/timing 继续锁定。
- 新版本计划为 `docs/experiment_management/plans/StackPyramid_ID_Recovery_512.md`，manifest 为 `configs/stackpyramid_id_recovery_512_manifest.json`。新增 repaired-Oracle ID seed `886300--886555`，与原 `886000--886255` 不重叠；旧 v1/v2/v3、retry1--retry4 和失败 checkpoint 均排除。
- 只有新的 256-ID collection、512-ID merge/audit、canonical-base fresh 20k training、selection-only 20-seed probes 和独立 formal 100-ID gate 全部按协议完成后，才决定是否解锁 OOD；formal gate `<80/100` 或任一证据缺失即保持下游锁定。

### 2026-08-19：StackPyramid ID Recovery 512 provenance correction and restart

- 上一条 `ORACLE_SMOKE_FAILED_BLUE_STAGE` 结论已撤回为 engineering diagnostic。逐帧/HDF5 对齐 canonical 280-action trajectory 后确认：red release约在step120，blue grasp约在step196，blue lift约在step210；当前 v4 runtime直接重放该canonical action在`886000/886280/886281/886300/886301`均成功。
- 根因是 collector planner path regression，而不是任务不可执行。active hybrid collector 让当前 reset 的 planner 生成 red single-grasp/lift/direct-transport/release prefix，只复用历史 step130后的 blue suffix；因此不把固定template伪装成新数据。
- v3 `retry10` smoke 已通过：`886280/886281` strict success，red release、blue grasp/lift事件完整，action lengths为`299/294`。当前 controller 正在新 `id_collection_additional_256_retry2` 中采集`886300--886555`；template-only v2 partial保留为`TEMPLATE_REPLAY_ONLY_DIAGNOSTIC`，不得merge/train，OOD/PCA/timing继续锁定。

### 2026-08-19：StackPyramid 512 X-VLA adapter/exposure hard gate

- Leader要求在 collection 完成后暂停训练启动。只读审计确认 canonical base config 为 `ee6d/20D/30 actions/30 domains`，而 StackPyramid H5 与 ManiSkill `pd_joint_pos` 是 `8D` joint-position targets；当前 adapter 改为 `auto/real=8/max=20/num_actions=10`，只对前8维计算 loss，再 trim 回8D执行。该接口 shape smoke 可运行，但语义尚未被官方 EE6D contract验证。
- 当前 training/eval/collector均硬编码 `domain_id=0`；官方README只定义 domain id 为 embodiment identifier，没有StackPyramid mapping或新soft-prompt验证。因此 merge、2-step training smoke、20k training、selection和formal gate继续锁定，直到 domain/action semantics audit通过。
- Exposure report必须用实际512 H5 anchor总数计算：20k steps、batch8对应160,000 sampled anchors，effective passes为`160000/N_anchors`；freeze=1000、warmup=2000仅先记录为5%/10%。审计报告为 `docs/experiment_management/diagnostics/StackPyramid_512_XVLA_adapter_audit.md`。

### 2026-08-19：StackPyramid 512 collection/exposure audit complete

- v3 full-planner collection `id_collection_additional_256_retry3` 已完成 `256/256` accepted、raw `256/256` success，独立 collection audit PASS；新增 anchor=`66,808`、tail anchors=`2,304`，动作长度跨 seed 变化，red/blue event evidence完整。
- 只读 projected merge 统计：旧256 anchors=`72,171`，预计512 anchors=`138,979`，tail=`4,608`；20k steps、batch8对应 `160,000` samples，即 `1.1513` effective passes；freeze/warmup比例为5%/10%。H5尚未merge。
- domain mapping和8D joint vs 20D EE6D semantics仍未解决，因此当前只写 exposure report，未启动 merge、2-step training smoke、continuation或formal gate。机器报告位于 v3 durable root 的 `training_exposure_report.json` 与 `action_diversity_report.json`。

## 3. 正式实验的不可变规则

### 3.1 任务和数据

- 每个 stage 先冻结任务语义、ID/OOD 因素、阶段 predicate、reset invariant、Oracle success 定义和 seed manifest。
- ID policy 只用 ID demonstrations 训练；不能用旧几何任务的 checkpoint 代替新任务 base。
- OOD 必须仍然可由 Oracle 完成。Oracle 失败说明任务设计无效，不说明 policy 的 OOD 泛化失败。
- mixed-stream gated collection 使用严格交替的原始 ID/OOD attempts；不设置 accepted 50/50 配额，也不为了凑比例专门补 ID。
- Internal-feature PCA 的 accepted 轨迹应自然 OOD-dominant；若接近均分，先审计 base ID/OOD SR、Oracle、calibration、threshold、norm、split 和 trigger，再决定是否停闸。
- 多阶段任务只训练一个 ID base；OOD1、OOD2、OOD3 必须从同一个 immutable ID base 独立收集、独立训练、独立评估，不混合、不串行续训。
- timing sweep 是 OOD-only，用于隔离接管时机；不能用 mixed-stream collection 代替 timing sweep。

### 3.2 训练和边界

- 所有 VLA BC/SFT 必须保留每个真实 observation anchor，包括 episode 尾部 anchor。
- 跨越 episode 边界的 action chunk 可以用最后真实动作做 padding，但必须使用 temporal validity mask；padding 不得进入 loss 分母。
- 正式训练前必须报告 anchor 总数、tail-anchor 数量、mask validity 分布，并完成 2-step checkpoint reload/forward smoke。
- 四方法比较必须使用同一 ID base、同一 OOD 定义、同一专家预算、相同训练配置和 held-out seeds。
- 专家预算按真实低层 expert actions 匹配，不按轨迹条数假设公平。
- 任何 gate 的 detector 评测都必须是 passive：评测时不让 detector 改变 rollout 动作；阈值只由独立成功 ID calibration 得到。

## 4. Agent 分工和认领规则

### 4.1 总控模式：目标授权，而不是逐条遥控

总控只下达四类信息：目标、不可违反的科学约束、完成定义和需要回报的证据。Agent 自主决定具体命令、代码组织、诊断顺序和资源安排，并对自己的执行结果负责。

- Agent 可以自主选择合理的修复方案、补充 smoke、调整等待和监测节奏。
- Agent 可以在当前实验目标内自行处理路径、依赖、视频落盘、进程恢复等工程问题。
- Agent 发现结果不符合预期时，应先自主提出原因排序和下一步实验，而不是停在简单汇报。
- Agent 不得自行改变任务语义、success predicate、ID/OOD 定义、专家预算口径或把 diagnostic 结果升级为 canonical。
- 只有涉及研究假设、共享 geometry、跨 Agent 资源冲突、正式结果纳入主表或不可逆的大规模资源消耗时，才需要总控介入。

每次回报重点回答四件事：现在处于哪个阶段、证据说明什么、下一步准备自主做什么、什么条件会让它停下并请求总控判断。

### 项目总控（Codex）

- 负责解释实验结果是否支持论文假设，批准或拒绝阶段转换，协调 Agent，维护本文件和结果索引。
- 任何与共享 task geometry、success predicate、norm、base checkpoint 或预算相关的冲突，由总控裁决。
- 不因为一个中间进程结束就宣布整条 pipeline 完成；必须验收当前产物并推进已批准的下一阶段。

### StackPyramid timing Agent

- 是 StackPyramid v4 geometry、Oracle、stage predicate 和 timing gate 的唯一 owner。
- 在 full gate 结束前可以修复实现问题，但每次修复必须说明一个根因、一个最小改动和一个针对性 smoke。
- 一旦 full gate 开始，不再继续漂移几何；只允许修复会阻止复现的工程错误。
- 完成后必须发布：源码 commit、task spec、seed manifest、Oracle gate report、ID collection/norm、immutable ID base、base/prefix gate report。

### StackPyramid 四方法 Agent

- 在 Timing Agent 发布唯一冻结资料前保持 blocked，不改 geometry，不运行自己的 v4，不使用 v3 或候选版本。
- 解锁后原样复用唯一 v4 source，分别完成 Internal PCA、Diff-DAgger、Failure-Recovery、Offline BC 四组；四组独立但共享协议。
- 任何一组门禁失败时保留 diagnostic，不能用 retry 配额掩盖问题。

### OpenDrawer Agent

- 只负责当前独立 ID-base controller；先满足 ID gate，再进入 OOD collection 和 matched-budget update。
- 到达一个评测 checkpoint 后，先核对 summary 和视频数量，再决定是否进入下一步；不能仅凭 loss 或 checkpoint 文件存在宣布通过。

## 5. 每次唤醒的总控流程

每次 30 分钟检查不是重复报数，而是按以下顺序做判断：

1. 读取三个 Agent 的最新实际响应、PID/控制器状态和当前阶段 marker。
2. 对照本文件、`共享实验协议.md`、对应 plan 和实验总计划，确认 Agent 没有越过依赖门。
3. 若长任务稳定：只检查真实吞吐、GPU/磁盘、最新产物和错误信号，不打断任务。
4. 若出现错误：先判断是环境/路径/代码问题还是科学门禁失败；可恢复的工程问题立即修复，科学门禁失败则停在当前阶段并保留证据。
5. 若阶段完成：审计 summary、分母、视频/动作边界、配置 provenance；通过后在同一次唤醒中启动并验证已批准的下一阶段。
6. 更新本文件的状态和研究日志；只有发生实质进展、修复、阶段转换或需要用户决策时才向用户报告。

监测节奏：启动、恢复、阶段切换和接近门禁时 5--10 分钟；确认稳定的小时级训练/收集/评测后 20--30 分钟。短 smoke、import、静态审计不建立监测任务，直接完成并判定。

## 6. 采用的 AutoResearch 工作方式

这里借鉴 AutoResearch 的“固定评估器 + 单变量迭代 + 机器可读结果 + 保留/回退”思想，但不把研究变成盲目搜索：

- 每个阶段必须有明确目标、约束、通过条件、输出目录和停止条件。
- 每轮只处理一个可解释的假设；先写证据，再改动；改动后做针对性验证。
- evaluator 必须输出 `pass/fail`、关键分数、分母、配置版本和证据路径。
- 只有 verifier 通过后才允许把结果推进到下一阶段；失败产物保留为 diagnostic，不覆盖旧结果。
- 任何“修复”如果改变 task geometry、success predicate、数据来源或训练口径，必须重新走前置 gate，不能当作普通 bug fix 混入主结果。

## 7. 下一步执行顺序

### 阶段 A：收尾当前前置门

1. Timing Agent 完成当前 v4 ID、Stage1、Stage2、Stage3 的 100-seed Oracle gate。
2. 如果 Oracle gate 全部通过，重新采集 v4 ID demonstrations，重新计算 ID norm，并训练唯一 v4 ID base；不得复用 v3 checkpoint。
3. 完成 ID/OOD base-policy gate 和 stage prefix/locality gate。任一失败就停闸并回到任务设计诊断。

### 阶段 B：启动正式分支

1. Timing 分支：在每个 OOD stage 上完成 OOD-only 的 Immediate、Previous-Stage Complete、Capability Boundary、Failure-Recovery timing sweep，并做 matched-budget update。
2. 四方法分支：从同一 immutable ID base 分别运行 Internal PCA、Diff-DAgger、Failure-Recovery、Offline BC；各 stage 独立，不串行续训。
3. 两个分支完成后，统一做 detector、intervention/timing diagnostic 和 downstream ID/OOD policy evaluation。

### 阶段 C：OpenDrawer

1. 保持当前 ID base 训练到 step 4000。
2. 完成独立 ID gate；只有达到协议要求才开始 OOD stage gate。
3. OOD collection、四组 matched-budget update 和评测必须在新 protocol-v2 根目录中进行，旧 v1 不混入。

## 8. 研究日志

### 2026-08-15：接手总控

- 读取了共享实验协议、持续运行协议、StackPyramid v4 规范、timing sweep 计划、实验总计划和结果索引。
- 读取当前论文 PDF 与两份最新阶段汇报 PPT，确认论文主线是 `data selection + takeover timing + expert data utility`，而非单独追求 detector 分数。
- 确认 Timing Agent 是 StackPyramid v4 唯一 owner；四方法 Agent 当前 blocked，避免 geometry 分叉。
- 确认 OpenDrawer 2000 步 ID gate 未通过，但训练本身稳定，暂不启动 OOD。
- 新增本总控文档，后续所有阶段转换和异常解释都记录在此。

### 2026-08-15 14:51：v4 Oracle gate 完成

- Timing Agent 报告 ID、Stage1、Stage2、Stage3 四个 v4 Oracle split 均为 `100/100`，reset invariant 和事件顺序失败均为 0。
- 新 ID demonstration 采集已经启动过，但直接向挂载结果盘写 MP4 时出现 FFmpeg `Invalid argument`，视频缺少 `moov`，因此整批只能标为 diagnostic。
- 总控要求改为本地临时盘采集，先完成单视频可解码 smoke，再重启正式 ID demonstration 采集；未通过视频、动作/帧边界和 metadata 验收前，不得重算 norm 或训练 base。

### 2026-08-15 15:21：v4 ID 数据验收与 base 训练

- 本地单视频写入/退出后解码 smoke 通过，约 30.3 秒；随后正式 ID 采集完成：129 次尝试得到 128 条成功 demonstration，1 条失败保留在分母中。
- 全量验收通过：128 条 HDF5，8D action，动作长度约 295--313；每条 observation 比 action 多 1 帧，base/hand 双相机与 state 对齐；129 个 raw MP4 全部可解码。
- 2-step temporal-mask SFT 和 checkpoint reload/forward smoke 通过；首次 reload 脚本缺少 bf16 autocast，已按训练环境重做并确认不是模型/数据故障。
- 唯一 v4 ID base 已启动 10k 训练，当前约 step 400，loss 有限并下降，单卡运行稳定。尚未启动 OOD collection、PCA calibration、timing sweep 或四方法训练。

### 2026-08-15 15:51：训练阶段持续稳定

- StackPyramid 唯一 v4 ID base 已推进至约 step7850，最近 loss 约为 `0.07--0.77`，step 7000/7500 checkpoint 已保存；未见 OOM、NaN 或 Traceback。
- 已要求 Timing Agent 在 step10000 完成后切换到短间隔门禁核对，先验收 checkpoint，再执行独立 ID gate 和 Stage1/2/3 prefix/locality gate；不会直接进入 OOD。
- OpenDrawer 已推进至约 step3500，step4000 checkpoint 尚未落盘，训练和 GPU 状态正常；四方法仍保持 blocked。

### 2026-08-15 16:21：两个 ID gate 进入收尾

- StackPyramid 10k ID base 正常退出，`ckpt-500` 至 `ckpt-10000` 均存在，最后 loss 为有限值 `0.0872`。
- 一条未显式固定 v4 selector 的 evaluator smoke 已标为 diagnostic；重新固定 selector 后 reset/event smoke 通过。20 条 v4 ID probe 全部抓到红块，但 strict success 为 `0/20`，说明当前 base 尚未掌握完整红块放置/金字塔任务。
- 正式 StackPyramid ID gate 已跑到 `78/100`，当前仍无 strict success；要求跑完整分母后写出明确失败证据，不得通过修改几何或 success predicate 继续推进。
- OpenDrawer step4000 checkpoint 已完整并进入独立 ID gate；当前未见 strict success，Agent 已收紧控制器，gate 失败后停止后续 OOD。

### 2026-08-15 16:51：进入自主修复阶段

- StackPyramid 旧 v4 base gate 最终为 `0/100` strict success；Agent 自主完成数据覆盖、evaluator 事件和语言指令对照，判断最小可解释问题是训练/评测 instruction 不一致，而不是几何或 success predicate。已从同一纯预训练模型启动只修正 instruction 的新 10k ID base，当前约 step200。
- OpenDrawer 旧 step4000 base 已停止并保留为 diagnostic；resolved config 审计确认实际为 `global batch=64 / micro=4`，不符合协议。Agent 已完成正确 `global=128 / micro=32 / single placement / ID norm` 的 2-step smoke，loss 和 temporal mask 指标均有限，准备从新目录重训 ID base。
- 两个修复都没有启动 OOD 或 gated update；四方法继续等待新的 immutable ID base。

### 2026-08-15 16:45：StackPyramid v4 base gate 失败，停止下游流程

- v4 Oracle preflight 已通过且保持唯一版本：源码 commit `28ad030`，task spec 为 `configs/stackpyramid_v4_task_spec.json`，seed manifest 为 `configs/stackpyramid_timing_v4_seed_manifest.json`，四 split 均为 `100/100` strict success，reset invariant 和事件顺序失败均为 0。
- ID collection 的直接 OSSFS MP4 写入故障已被隔离：旧 `id_collection_retry1` 和中途停止的本地 retry 仅作 diagnostic；本地单视频 smoke 在退出后可解码，正式 `id_collection_retry4_local` 完成 129 次尝试、128 条成功 demo、1 条失败，129 个 raw MP4 全部可解码，HDF5 observation/action、双相机和 state 边界通过。正式同步目录为 `/mnt/data/ask4help/results/xvla_stackpyramid_v4/id_collection_retry4/`。
- 唯一 v4 ID base 从纯预训练 `/mnt/data/ask4help/models/X-VLA-Pt_from5090_v4` 训练 10000 步；`ckpt-500` 至 `ckpt-10000` 均保留，最后 loss `0.087214`，201 条日志 loss 记录均有限，Traceback/OOM/NaN 均为 0。训练归档为 `/mnt/data/ask4help/results/xvla_stackpyramid_v4/id_sft_10000_retry1/`。
- 正确 v4 selector 下的完整 ID gate 使用 seeds `84400--84499`：`89/100` ever-grasped、`51/100` red-lifted、`4/100` base-completed、`0/100` red-placed、`0/100` strict success；100/100 视频可解码。报告和 marker 位于 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_v4_100/`。
- 诊断结论是基础策略能学到部分抓取/抬升，但没有学会稳定的红块放置，因此不满足 ID `>=80/100` gate。该结论不改变几何、不改变 success predicate；不启动 OOD collection、PCA calibration、timing sweep 或四方法训练。v4 Oracle/数据/训练全部保留，但当前 base 不得进入 canonical 主结果。

### 2026-08-16：语言协议修复后的 ID base retry

- 对 128 条 ID Oracle HDF5 做了阶段覆盖审计：red lift、red placement、blue lift 均为 `128/128`，典型事件约在低层动作 60、137、233，排除了 Oracle 或训练数据缺少放置阶段的解释。
- 对同一旧 `ckpt-10000` 做了仅改变语言指令的 20 条对照：训练指令 `on` 下为 `0/20` strict success、`1/20` red placement；canonical `next to` 下此前为 `0/20`。该结果支持指令错配是有效工程根因，但也表明它不是唯一因素。
- 已将训练脚本统一为 canonical `stack the red cube next to the green cube and place the blue cube on top`，本地提交并推送为 `562d615`；远端仅同步该文件，保留其他 Agent 的未提交 v4 修改。
- 新 prompt-consistent ID base 从纯预训练模型重新启动，输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_sft_10000_promptfix_retry1/`；2-step temporal-mask/reload smoke 通过，当前约 step3650，loss 有限且 checkpoint 持续落盘。首次 ID gate 和所有 OOD 下游仍锁定。

### 2026-08-16：v4 任务收尾审计

- 再次确认 `v4_preflight_release.json` 的 `downstream_unlocked=false`，ID gate 报告已同步至持久目录 `/mnt/data/ask4help/results/xvla_stackpyramid_v4/base_gate_id_ckpt10000_v4_100/`，其中保留 100 条 gate 视频和 `BASE_POLICY_NOT_ACCEPTED` marker。
- 服务器上发现的旧 v3 recovery 控制器处于停止态，唯一子进程已退出，且不属于 v4 正式流程；已清理该失效控制器，未删除任何实验产物。v4 后续 OOD、PCA、timing 和四方法流程仍保持锁定。

### 2026-08-16：canonical 指令修复后的 ID base 与独立 gate

- 新 ID base 在保持 v4 geometry、success predicate、ID/OOD 定义和 temporal-mask 协议不变的前提下完成 10,000 步；`ckpt-500` 至 `ckpt-10000` 均已写出，最终训练 loss 为有限值。
- `ckpt-10000` 的 bf16 reload/forward smoke 通过：loss finite，`38,950` 个 anchors 和 `1,152` 个尾部 mask anchors 均被保留，最终 observation 仍有 1 个有效 target。
- 独立 ID gate 的第一次 retry 使用了未写入冻结 manifest 的 `84600--84699` 区间，已保留 28 条并标记为 diagnostic，未进入正式门禁。随后已停止该批，并按冻结 v4 manifest 的 `84400--84499` 区间在全新 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_promptfix_retry3_100` 重新启动完整 gate；其 `seed_manifest.json` 已随结果目录保存。
- 因此当前仍禁止 OOD collection、PCA calibration、timing sweep 和四方法训练；旧 gate 与新 gate 的部分结果均不进入正式主表。

### 2026-08-15 18:21：新 base gate 仍未通过，转入有限闭环诊断

- canonical 指令修复后的新 ID base 完整 gate 已完成：`0/100` strict success、`95/100` ever-grasped、`46/100` red-lifted、`0/100` red-placed；100 个 gate 视频已生成。
- Agent 在原始 training seed 上复现了“能抓取/抬升但不能放置”，因此将问题从 held-out 泛化进一步缩小到抓取后的 transport/release 或 action-chunk 执行；action-dimension 接口和训练数据阶段覆盖没有发现明显错位。
- 当前仅运行一轮有限 `execute_horizon`/chunk 诊断，结果不进入正式结果；在得到可用 ID base 前，OOD、PCA、timing 和四方法仍锁定。
- OpenDrawer 新配置训练约 step461，loss 和 mask-valid ratio 正常，首个 checkpoint 尚未完成。

### 2026-08-16：checkpoint-selection probe 审计与新 base 训练进行中

- 诊断 probe `checkpoint_selection_seed84200_ckpt2000_id20_retry1` 已正常完成，而非异常退出：输出中存在 `EVAL_COMPLETE`、`summary.json` 和 20 个视频，summary 记录 `20` 条评测、`0/20` ever-grasped、`0/20` base-completed、`0/20` strict success。PID 文件 `/root/ask4help_stage2_logs/stackpyramid_v4_checkpoint_selection_seed84200_ckpt2000_id20_retry1.pid` 仍存在但对应进程已退出，判定为陈旧 PID 文件；该 probe 仅作 step-2000 诊断，不改变正式门禁。
- canonical-instruction retry2 使用预先声明 seed `87004`，训练进程仍在稳定推进，最近已到约 `step8700/10000`，`ckpt-8500` 已写出，训练日志未见 Traceback、OOM、NaN 或 Exception。该候选仍标记为 diagnostic，待 10k checkpoint 完成后进行独立 ID gate。
- 在新 base 通过独立 ID 及 Stage1/2/3 prefix/locality gate 前，继续禁止 OOD collection、PCA calibration、timing sweep 和四方法训练。

### 2026-08-16：canonical-instruction retry2 的完整 ID gate

- 新 base `id_sft_10000_promptfix_retry2_seed87004_attempt2/ckpt-10000` 的独立冻结-manifest gate 已完整结束：`84400--84499` 共 `100/100`，输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_promptfix_retry4_100/`，存在 `EVAL_COMPLETE`、`summary.json` 和 `100/100` 视频。
- 完整分层结果为：`88/100` ever-grasped、`58/100` red-lifted、`3/100` red-placed、`4/100` base-completed、`0/100` strict success；运行配置仍为正式 `execute_horizon=5`、`flow_steps=5`、v4 geometry。日志未发现 Traceback、OOM 或 Exception。
- 因此该候选明确记为 `BASE_POLICY_NOT_ACCEPTED/ID_GATE_FAILED`，不得进入 Stage1/2/3 prefix/locality、OOD collection、PCA calibration、timing sweep 或四方法训练。相较 promptfix retry1 的 `0/100` strict success，本次 seed/训练重跑没有改变根本结论；当前最可能问题仍是闭环 transport/release，而非 OOD 几何或门禁口径。

### 2026-08-16：flow latent padding contract 修复诊断

- 进一步审计发现，自定义 SFT 脚本在 8D action 上采样 flow noise 后才补到 X-VLA 的 20D model width；而 evaluator 在完整 20D latent 上采样 noise。由此训练时 dummy dimensions 恒为 0、推理时却为随机噪声，形成明确的 training/inference contract mismatch。
- 最小修复只改变 `tools/run_stackpyramid_xvla_training.py` 的 masked flow loss：先通过 `action_space.preprocess` 补齐 model-facing target，再对完整 20D target 采样 noise；真实 8D loss、temporal mask、数据、几何、prompt、正式 execute horizon=5 均不变。修复已提交为 `9092666`，2-step checkpoint/reload smoke 输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/flow_padding_contract_smoke_retry1/` 并通过。
- 已从纯预训练模型启动新 ID-only diagnostic `id_sft_10000_flowfix_diag_retry1_seed87006`，PID `404778`，输出位于 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/`。该候选不覆盖旧 base，完成后仍须用冻结 `84400--84499` 做独立 ID gate；在 gate 通过前不启动任何 OOD/PCA/timing/four-method 阶段。
- 补充审计：PID `384936` 对应 `/root/ask4help_stage2_logs/stackpyramid_v4_checkpoint_selection_seed84200_ckpt2000_id20_retry1.pid`，进程已退出但输出存在 `EVAL_COMPLETE`、`summary.json` 和 20 个视频，结果为 `0/20` ever-grasped、`0/20` base-completed、`0/20` strict success，判定为正常完成后的陈旧 PID 文件，而非异常退出。另一个 ckpt-10000 诊断 probe `/root/ask4help_stage2_work/xvla_stackpyramid_v4/checkpoint_selection_probe_ckpt10000_id20/` 也有完整 marker、summary 和 20 个视频，结果为 `18/20` ever-grasped、`0/20` strict success；两者均不进入正式 checkpoint 选择或 ID gate。
- flow-fix 诊断训练已完成并通过 reload smoke；其新的冻结-manifest ID gate 位于 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_flowfix_diag_retry1_100/`，PID 文件为 `/root/ask4help_stage2_logs/stackpyramid_v4_base_gate_id_ckpt10000_flowfix_diag_retry1_100.pid`。评测已完整结束：`100/100` 视频、`EVAL_COMPLETE`，无 Traceback、OOM、CUDA error 或 NaN；分层结果为 `65/100` ever-grasped、`35/100` red-lifted、`0/100` red-placed、`0/100` base-completed、`0/100` strict success。因此 flow-latent 宽度修复消除了明确的训练/推理契约问题，但未解决红块运输/放置闭环能力，候选仍记为 `BASE_POLICY_NOT_ACCEPTED/ID_GATE_FAILED`，所有 OOD/PCA/timing/four-method 阶段继续锁定。
- flow-fix 的受控 ID horizon 对照位于 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/diagnostics/flowfix_horizon_compare_retry1/`：同一 `ckpt-10000`、同一五个 seed 在 execute horizon 1/5/10 下均为 `0/5` grasp、`0/5` red-lift、`0/5` strict success，支持问题仍在闭环任务能力而不是单一重规划 horizon；同一 seed 的两次 horizon=5 重跑结果完全一致。旧 evaluator 在这轮诊断中把某些被推动到邻近位置的 red cube 在第 1 步记为 `red_placed`，但并未经历 grasp/lift；这不影响最终 success predicate，却会污染 stage-locality 事件。已用提交 `ef28714` 修正为只按 `red_grasped -> red_lifted -> red_placed -> blue_grasped -> blue_lifted` 的已完成前置事件记录，并用 1 条 ID smoke 验证 reset 后不再误报 `red_placed`。该修复只影响事件记录，不改变任务几何或最终成功定义。
- 在不改变 v4 geometry、success predicate 或 formal execute horizon=5 的前提下，启动了唯一一条 action-side-only diagnostic：`freeze_steps=10000`，从原始 X-VLA checkpoint、同一 128 条 ID 数据和 flow-latent 修复脚本重新训练，输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_sft_10000_flowfix_vlm_frozen_diag_retry1_seed87007/`，PID `425310`。2-step smoke 已通过；正式诊断已推进到约 `step8600/10000`，`ckpt-8500` 已存在，最近 loss 为 `56.97` 且 finite，未见实际 Traceback/OOM/CUDA/NaN，完成后仍须独立 ID gate，期间不启动 OOD/PCA/timing/four-method。

### 2026-08-16：action-side-only diagnostic 完成，进入独立 ID gate

- `id_sft_10000_flowfix_vlm_frozen_diag_retry1_seed87007` 已完成 `10000/10000` 步，输出存在 `ckpt-10000` 与 `TRAINING_COMPLETE`；最终记录 loss 为 `49.7946`，训练进程正常退出，日志未见实际 Traceback、OOM、CUDA error 或 NaN。
- 该候选保持 v4 geometry、success predicate、冻结 manifest `84400--84499` 和正式 `execute_horizon=5` 不变，仅验证 `freeze_steps=10000` 的单一训练假设。当前已启动其独立 ID gate；在 gate 通过前继续禁止 Stage1/2/3 prefix/locality、OOD collection、PCA calibration、timing sweep 和四方法训练。

### 2026-08-16：action-side-only diagnostic 的独立 ID gate 结论

- 独立 ID gate 输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_flowfix_vlm_frozen_diag_retry1_seed87007_100/` 已完成 `84400--84499` 全部 `100/100`，存在 `EVAL_COMPLETE`、`summary.json`、`episodes.jsonl` 和 `100/100` 视频。
- 分层结果为 `0/100` ever-grasped、`0/100` red-lifted、`0/100` red-placed、`0/100` blue-grasped、`0/100` blue-lifted、`0/100` base-completed、`0/100` strict success。没有实际 Traceback、OOM、CUDA error 或 NaN；Transformers 的 `GenerationMixin` 仅为已知 warning。
- 因此该单一 `freeze_steps=10000` 假设未恢复 ID 基础能力，候选明确记为 `BASE_POLICY_NOT_ACCEPTED/ID_GATE_FAILED`。不得进入 prefix/locality gate、OOD collection、PCA calibration、timing sweep 或四方法训练；现有 v4 正式主流程仍无可用 ID base。

### 2026-08-16：VLM-frozen / transformer-core-enabled 区分诊断启动

- action-side-only gate 显示全程只训练 action heads 不足以恢复基础能力；代码审计确认 `freeze_steps=10000` 同时让 transformer core 的学习率保持为 0，因此不能区分 VLM 与 core 的作用。
- 在不改变任务语义、几何、success predicate、数据、manifest 或 formal `execute_horizon=5` 的前提下，增加可选 `--freeze-vlm` 开关：VLM 始终冻结，transformer core 与 action heads 在原 `freeze_steps=1000` 后更新。该开关已提交并推送为 `2cd9a03`，远端 `py_compile` 和 2-step smoke `/root/ask4help_stage2_work/xvla_stackpyramid_v4/flowfix_vlm_frozen_core_smoke_retry1/` 已通过，loss finite。
- 已启动新的唯一 ID-only 诊断；完成后仍须用冻结 `84400--84499` 做独立 ID gate，期间不启动 prefix/locality、OOD collection、PCA calibration、timing sweep 或四方法训练。

### 2026-08-16：VLM-frozen / transformer-core-enabled 训练完成

- `id_sft_10000_flowfix_vlm_frozen_core_diag_retry1_seed87008` 已完成 `10000/10000` 步，存在 `ckpt-10000` 与 `TRAINING_COMPLETE`；最终 loss 为 `0.4179`，训练日志未见实际 Traceback、OOM、CUDA error 或 NaN。
- 该结果只说明 core-enabled 训练过程恢复得更好，不能替代独立 ID gate。现已启动同一冻结 manifest `84400--84499`、正式 `execute_horizon=5` 的 100 条 ID gate；在其结论前继续锁定所有 OOD/PCA/timing/four-method 阶段。

### 2026-08-16：VLM-frozen / transformer-core-enabled ID gate 结论

- 独立 gate 输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/base_gate_id_ckpt10000_flowfix_vlm_frozen_core_diag_retry1_seed87008_100/` 已完成 `84400--84499` 全部 `100/100`，存在 `EVAL_COMPLETE`、`summary.json`、`episodes.jsonl` 和 `100/100` 视频。
- 最终 success 分层为 `29/100` ever-grasped、`22/100` base-completed、`11/100` strict success。无实际 Traceback、OOM、CUDA error 或 NaN；仅有已知 Transformers warning。
- core-only 训练相较 action-side-only 的 `0/100` strict success 有改善，但仍未达到 ID gate 要求；同时 ordered-event 计数为 `2/100` red-grasped、`1/100` red-lifted、其余为 0，与最终 success 的状态统计不一致，因此该结果只能作诊断，不能进入 stage-locality。候选记为 `BASE_POLICY_NOT_ACCEPTED/ID_GATE_FAILED`，所有 OOD/PCA/timing/four-method 阶段继续锁定。
- 目前已完成 prompt、flow-latent、action-side-only、VLM-frozen/core-enabled 四条受控诊断；在不改变训练语义、success predicate 或任务几何的前提下没有可继续自动尝试的最小修复，后续需要重新决定 ID base 的训练协议或任务难度。

### 2026-08-16：StackPyramid v4 ID-base recovery-256 接管

- 根据新的批准方案，当前唯一 owner 改为 `codex-stackpyramid-v4-id-recovery-256`。下一阶段顺序固定为：审计现有 128 条 ID 数据与 norm provenance；若审计通过，在同一 v4 geometry/spec/predicate 下新增约 128 条成功 ID demonstration；重新计算冻结 norm，从纯预训练 X-VLA 重新训练至最多 20k；用独立 20-seed probe 选择 checkpoint；最后用 `84400--84499` 做独立 ID gate。未通过前不得启动 OOD、PCA、timing 或四方法。
- 2026-08-16 接管时 H20 SSH 在 banner 阶段连续超时，尚未执行新的服务器命令；既有诊断产物全部保留，待连接恢复后从 128 条数据机器可读审计开始。

### 2026-08-16：批准一次增加 ID 数据与训练覆盖的受控候选

- 项目总控决定继续选择 StackPyramid，但不使用任何 `ID_GATE_FAILED` checkpoint，也不启动 OOD、PCA、timing 或四方法流程。
- Timing Agent 被授权先审计现有 v4 的 128 条 ID demonstration；若动作/帧边界、双相机与 state 对齐、temporal-mask anchor、canonical instruction、阶段覆盖、视频和 norm provenance 全部通过，则在完全相同的 v4 ID 分布上扩充到约 256 条总量。
- 新候选必须从同一纯预训练 X-VLA base 重新训练，保持 v4 geometry、Oracle、success predicate、formal horizon、flow-latent contract、temporal mask 和完整尾部 anchor 不变；训练上限可扩至 20k，并使用独立 selection probe 后再用冻结 `84400--84499` 做正式 ID gate。
- 该实验只验证“ID 样本量与训练/部署覆盖不足”这一受控假设，产物暂记为 diagnostic。只有严格 ID gate 达到协议门槛并通过 prefix/locality gate，才允许解锁 OOD/PCA/timing；否则保留完整证据并停止下游。

### 2026-08-16：256-data recovery 采集发现 selector 错误并纠正

- 原计划新增的第一批 128 条采集未显式设置 `STACKPYRAMID_OOD_GEOMETRY=v4`，实际落入旧默认几何；审计发现 red-green 初始距离约 `0.03--0.04 m`，不符合 v4 约 `0.16 m` 的冻结几何。该批全部保留为 diagnostic，禁止合并、训练或覆盖。
- 已用显式 v4 selector 完成 smoke：初始 red-green 距离 `0.1597 m`，red lift 真实发生；随后重新启动全新目录的 v4 ID 扩充采集。当前约 `48/128` accepted，进程健康，曾出现的 screw-plan fallback 已保留在 raw 记录中。
- 已新增 256 条 ID 数据合并工具并提交为 `11b4065`；在正确 v4 扩充采集完成并通过 HDF5/MP4/metadata/temporal-mask 审计前，不启动训练、OOD、PCA 或 timing。

### 2026-08-16：256-data recovery 审计、smoke 与 fresh training 启动

- 正确 v4 扩充目录 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_collection_extra128_retry3_v4/` 已完成，summary 为 `128/128` accepted、`128/128` raw successes、`38927` expert action steps，并存在 `COLLECTION_COMPLETE`。此前未设置 v4 selector 的 `id_collection_extra128_retry2` 仍为错误几何 diagnostic，未被使用。
- 独立审计输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_collection_extra_audit_retry2_v4/` 通过：HDF5 `128` groups、边界错误 `0`；视频 `128/128` 可解码；raw attempts/success/failure 为 `128/128/0`；state-derived red lift、red placement、blue lift 均为 `128/128`；tail anchors `1152`，valid target `1..9` 各 `128`，`10` 为 `37775`。norm provenance 为 `xvla_action_space`，无外部 norm asset。
- 第一次自动 provenance 路径推断生成的合并目录 `id_training_collection_256_retry1` 仅作 diagnostic；未将其用于训练。修正工具后，全新正式合并目录为 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_training_collection_256_retry2/`，明确记录原始 collection `129 attempts/128 successes/1 failure` 与新增 collection `128/128/0`，256 groups，ID-only、无 OOD。
- 全新 256-data smoke 输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_sft_256_smoke_retry2/` 通过 2-step temporal-mask/reload/forward：`77877` anchors、`2304` tail anchors、每个 episode 最后 anchor 恰有 1 个 valid target，reload loss `140.3488` 且 finite。`retry1` 曾因环境缺少 X-VLA 官方 requirements 中的 `fastapi` 退出，已按 `/root/X-VLA/requirements.txt` 补齐并通过 import/GPU smoke；该失败目录仅作诊断。
- 已从纯预训练 `/mnt/data/ask4help/models/X-VLA-Pt_from5090_v4` 启动新的 ID-only fresh training：PID `531385`，日志 `/root/ask4help_stage2_logs/id_sft_20000_recovery256_retry1.log`，输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_sft_20000_recovery256_retry1/`，20,000 steps、每 2,000 保存、batch 8、learning rate `2.5e-5`、freeze 1,000、warmup 2,000、seed `87101`。启动期 step 1 loss `162.7582` finite，GPU0 正常，GPU1 空闲。
- 训练完成后仍须先用独立 20-seed selection probe 选择 checkpoint，再以冻结 `84400--84499` 做正式 ID gate；ID gate 与 prefix/locality 全部通过前，不得启动 OOD collection、PCA calibration、timing sweep 或四方法训练。

### 2026-08-16：256-data training 完成，selection 与 ID gate 已接续

- `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_sft_20000_recovery256_retry1/` 已完成 `20000/20000` 步，存在 `TRAINING_COMPLETE` 与 `ckpt-2000` 至 `ckpt-20000`；最后训练 loss 为 `0.100604`，最近日志 finite。训练配置仍为 v4 ID-only、256 条 merged episodes、batch 8、每 2k 保存、seed `87101`，从纯预训练 X-VLA base 启动。
- 训练数据 provenance 已核对：`id_training_collection_256_retry2/collection_provenance.json` 明确为原始 v4 ID `128` 加正确新增 v4 ID `128`，`ood_included=false`，norm 为 `xvla_action_space` 且无外部 norm asset；anchor report 为 `77877` anchors、`2304` tail anchors，最终 observation 保留 1 个 valid target。
- 独立 checkpoint selection 已在全新目录 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_checkpoint_selection_recovery256_retry2/` 启动，PID `550994`，20-seed probe 使用 `88400--88419`，旧 retry1 仅作 diagnostic。首个 `ckpt-2000` probe 已开始写视频且进程健康。
- 评测器此前在 action-expert BF16 autocast 路径触发 SIGFPE；对照确认 VLM 前向正常，故障集中在独立推理的 transformer 混合 dtype/运行时路径。当前已统一训练 action contract、固定评测线程池，并使用 FP32 inference；新 GPU simulator/render 5-step smoke `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_selection_eval_contract_smoke_retry9/` 已通过。对应源代码提交为 `5210ae1`，后续 selection 继续使用全新输出。
- 已启动阶段控制器 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_gate_recovery256_retry2/`，PID `552245`，等待 selection 完成后自动运行冻结 `84400--84499` 的 100-ID gate；只有 strict success 至少 `80/100` 才会继续 prefix/locality gate，未通过则写 `ID_GATE_FAILED` 并锁定 OOD/PCA/timing/four-method。

### 2026-08-16：Recovery-256 / 20k 候选 ID gate 收尾

- selection 已完整评估 `ckpt-2000` 至 `ckpt-20000`，每个 checkpoint 均为 `20/20` 视频且 strict success `0/20`；按既定规则（strict success、base completion、ever grasp、最后才是 step）选择 `ckpt-20000`。selection 输出为 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_checkpoint_selection_recovery256_retry2/`，该 probe 不作为正式 gate。
- 冻结 manifest `84400--84499` 的正式 ID gate 已完整结束：输出 `/root/ask4help_stage2_work/xvla_stackpyramid_v4/id_gate_recovery256_retry2/formal_id_gate/`，`EVAL_COMPLETE`、`summary.json`、`episodes.jsonl` 和 `100/100` 视频均存在；结果为 `strict_success=0/100`、`ever_grasped=99/100`、`red_lifted=76/100`、`red_placed=0/100`，`ID_GATE_FAILED` 已写入。阶段控制器已进入 `diagnostic_id_gate_failed`，无后续子进程；`ID_GATE_COMPLETE`、prefix/locality 完成标记和 downstream unlock 标记均不存在。
- 失败模式提供了新的 event-level 证据：最终红块仍被夹持 `99/100`，最终红绿平面距离均值约 `0.179 m`（范围约 `0.160--0.191 m`），明显未达到 v4 的 next-to 阈值约 `0.0616 m`。因此 256 条数据与 20k 训练仍未学会红块的 transport/release/placement 闭环；该判断与既有诊断一致，不修改 geometry、task spec、success predicate 或 horizon。
- Recovery-256/20k 候选明确记为 `BASE_POLICY_NOT_ACCEPTED/ID_GATE_FAILED`，所有 100 条 gate 视频、summary、logs 和 checkpoints 仅保留为 diagnostic。不得启动 prefix/locality、OOD collection、PCA calibration、timing sweep 或四方法训练。

### 2026-08-16：OpenDrawer step4000 ID gate 失败

- OpenDrawer 正确配置的 step4000 checkpoint 已完成独立 `100/100` ID gate，strict success 为 `13/100`，低于协议要求的 `80/100`。
- 已写入并核对 `/data/zhaozhixuan/Ask4Help-open-drawer/results/open_drawer_failure_detection_v1/id_base_protocol_v3_batch128/ID_BASE_PROTOCOL_FAILED`；base training/controller 已停止，step4000 评测保留 `100/100` episodes、`100/100` 视频、summary、checkpoint 和诊断日志；不得启动 OOD 或 gated update。
- StackPyramid 256-data/20k candidate 已完成 selection 与冻结 manifest ID gate，但 `strict_success=0/100`，已写入 `ID_GATE_FAILED` 并停止；全部下游 OOD、PCA、timing、prefix/locality 和四方法阶段继续锁定。该旧“训练中”描述仅保留为历史记录，不代表当前状态。

### 2026-08-17：StackCube Stage 2 timing audit 与 PCA/Diff data-selection 对照收尾

- 当前 owner 为 `XVLA Stack Hub`。既有 X-VLA StackCube Stage 2 timing sweep 已审计：四组为 OOD-only 的 `Immediate`、`Post-Grasp`、`Post-Lift`、`Failure-Recovery`，共同 full-suffix budget 为 `1968`，共同 checkpoint selection 选 step `2000`；最终 ID SR 为 `73/100、58/100、95/100、71/100`，Stage2 OOD SR 为 `52/100、10/100、4/100、30/100`。timing 目录的 DCA/EAS/DCE 只作诊断，不与 mixed-stream data selection 混写；旧 pipeline state stale 属元数据缺陷，timing 结果仍只限 timing 使用。
- PCA/Diff retry2 因直接写 OSSFS 的 `episodes.jsonl` 报 `OSError: [Errno 22] Invalid argument`，保留为 diagnostic。retry3 已通过本地 collector/write smoke 后启动，完整产物在 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stage2_data_selection_pca_diff_v1_retry3/`；PCA 100 accepted（89 OOD/11 ID）且视频、parquet、suffix、temporal-mask 审计通过。
- Diff retry3 在 raw `107`（ID `54`、Stage2 OOD `53`）后自然停止：冻结 threshold `0.0443928577`、patience `2` 下 `20` 条轨迹有单次越阈值但 `0` 条有连续两次越阈值，故 `0` accepted、无 matched budget、未训练、未评测。该结果说明当前固定 Diff trigger 在这批协议中没有形成真实 takeover，不允许改阈值凑数，也不允许把 PCA 单组结果写成完整 PCA-vs-Diff 对照。
- Stage E 未发现可复用的真实资产；已完成独立 logic/schema smoke，验证 upper/lower hysteresis、stable recovery、dwell/cooldown 和二次 takeover，结果位于 retry3 `bidirectional_gate_smoke/summary.json`，不声称 task success。当前 pipeline 停在 `DIAGNOSTIC_STOPPED_NO_MATCHED_DIFF_DATA`，下一步需先决定是否采用预先声明的 Diff trigger 修复协议，再开新的 diagnostic retry；不得从这批未匹配数据进入训练或主表。
- 为区分 Diff 的阈值问题与连续触发问题，另建全新 `diagnostics/diff_patience1_smoke/`，保持同一 checkpoint、calibration、q=.95 threshold 和 ID/OOD 交替流，仅设 patience=1；raw `16` 得到 `5` 条 accepted，全部 Stage2 OOD，`16/16` 视频可解码。该结果只证明 consecutive-trigger rule 是可疑根因，仍是 diagnostic，不改正式 retry3 的 patience=2，不进入 matched-budget training 或论文主表。
- 之后的 live-process audit 确认 retry2 controller PID `652535` 与 retry3 PID `666416` 均已退出，遗留 PID 文件为陈旧状态；retry2 的错误日志和 diagnostic 产物均保留，因无存活进程而无需停止。GPU PID `276925` 属于无关 `online-awbc` 进程，未触碰。正式 patience=2 的 `0` accepted 结果不变，patience=1 的 `5` accepted 仍仅作 trigger 根因诊断。
- 已在 `docs/experiment_management/plans/Diff_Trigger_Repair_Protocol.md` 预先声明唯一的 Diff trigger 修复：保持 base、ID calibration、norm、threshold、score、ID/OOD stream、expert budget 和 success predicate 不变，只将 temporal persistence 从 `patience=2` 改为 `patience=1`。该协议先做新目录 smoke；只有 smoke 有真实 query 且写入/视频审计通过，才允许开一次自然收集 retry。仍不设 accepted ID/OOD 配额，不训练或评测未通过数据审计的结果。
- patience=1 smoke 已在全新本地目录 `/root/ask4help_stage2_work/xvla_stackcube_diff_trigger_repair_v1_smoke_patience1/` 完成并通过审计：20 次 raw（ID/OOD 各 10），1 条 accepted（Stage2 OOD），真实报警与 takeover 位于 `env_step=75`，专家动作 26 步；20/20 视频可读，20/20 动作与 state 行数对齐，`SMOKE_AUDIT_PASSED` 存在。该结果只满足 collection 前置门，不改变 retry3 的正式 `patience=2` 结果。
- smoke 后复核通过才允许启动协议规定的一次自然 patience=1 collection；其余资产、阈值、stream、预算、success predicate 和训练/评测锁定条件不变。
- patience=1 自然 collection 已达到预声明停止条件：raw `620`（ID `310`、Stage2 OOD `310`，严格交替），accepted `100`（ID `10`、Stage2 OOD `90`），accepted expert actions `2126`。所有 accepted episode 均为成功且包含真实报警、takeover 和非空 expert suffix；结果没有按 ID/OOD 配额筛选。
- 新 collection 的完整审计位于 H20 `/root/ask4help_stage2_work/xvla_stackcube_diff_trigger_repair_v1_collection_patience1/audit/`，`COLLECTION_AUDIT_PASSED` 存在：`620/620` raw 视频可解码，`620/620` task-state 文件通过，`100/100` accepted parquet、action/state/frame 边界和 episode 链接通过。已同步到 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/diff_trigger_repair_v1_collection_patience1/`，`SYNC_COMPLETE` 存在。
- 审计保留一项 provenance 警告：collector 原始 `collection_provenance.json` 仍使用旧 format 名称，并未物化写入 patience、seed manifest、expert-action-budget 字段。全新 `audit/repair_protocol_manifest.json` 对预声明的 patience=1、冻结 checkpoint/calibration/threshold、交替 stream 和停止规则作了补充记录；该警告需在解锁前由总控确认。当前仍禁止训练、checkpoint selection 和 final evaluation，不把这批结果写入 PCA-vs-Diff 主表。
- 对现有 timing-only 产物补做了 intervention-quality 关联审计：Immediate/Post-Grasp/Post-Lift/Failure-Recovery 的 DCA 为 `1.000/0.437/0.397/0.180`，EAS 为 `0.000/0.402/0.587/0.109`，DCE 为 `0.000/0.399/0.454/0.104`；对应 Stage2 OOD SR 为 `0.52/0.10/0.04/0.30`。四个条件上的描述性 Spearman ρ 为 DCA `0.40`、EAS `-1.00`、DCE `-1.00`。由于样本点仅四个且 Immediate 是完整专家 suffix，结果不支持把这些诊断指标当作 learning utility 的充分代理；仅作为 timing 诊断补充，不升级任何主结果。

### 2026-08-17：Diff patience=1 repair v1 通过解锁并进入独立 matched-budget training

- 总控已接受的新协议版本为 `Diff patience=1 repair protocol v1`。原 patience=2 retry3 的 `0` accepted 结果、旧 patience=1 smoke 和所有 timing-only 产物均保持 diagnostic，未合并、未替换。
- 最终 provenance cross-check 位于 H20 `/root/ask4help_stage2_work/xvla_stackcube_diff_trigger_repair_v1_training_prep_matched_budget_434_retry1/audit/provenance_crosscheck.json`，`PROVENANCE_CROSSCHECK_PASSED` 与 `ACCEPTED_PROTOCOL_VERSION` 均存在；交叉核对了 task/base、ID-only calibration、q=.95 threshold、norm、score aggregation、patience=1、冻结交替 seed stream、success predicate、expert-action budget 和训练协议。
- PCA 与 Diff 使用同一 23 条共同成功 seed 的 full suffix，独立数据集各选取精确 `434` 个专家动作，未切短 suffix；source-balanced training 保持原始 ID replay:new expert `1:1`，batch `8`、gradient accumulation `4`、temporal mask、原始 ID norm 和相同优化配置。
- 独立 2-step train/reload smoke 位于 `/root/ask4help_stage2_work/xvla_stackcube_diff_trigger_repair_v1_sft_matched_budget_434_retry1/`，loss finite，`valid_action_ratio` 为 `0.80/0.875`，`SMOKE_COMPLETE` 存在。正式 Diff training 已从 immutable `ckpt-7500` 启动，PID `764871`、GPU1、输出 `formal_10000/`，启动阶段 step `0/20/40` 均为 finite；`TRAINING_STARTED` 已写入。训练完成前不得启动 final evaluation。
- 已补齐独立 PCA training-prep `/root/ask4help_stage2_work/xvla_stackcube_internal_pca_sft_matched_budget_434_retry1/prep/`：23 个完整 suffix、434 个专家动作、原始 ID replay 与新专家 1:1、同一 temporal-mask contract，`PCA_PREP_AUDIT_PASSED` 已写入。阶段控制器 PID `768163` 已启动，等待 Diff 训练完成后自动执行 PCA smoke、formal training、逐 checkpoint 归档、共同 selection 和 held-out evaluation；当前未启动 PCA formal 或 final evaluation。

### 2026-08-17：两条当前并行任务的总控收口状态

- **StackCube Stage 2**：上述训练衔接已实际完成。Diff 的 `TRAINING_COMPLETE` 与20/20归档均已核实；PCA formal training、20/20归档、共同 selection（step `2000/4000/6000/8000/10000`）和 held-out evaluation 均已落盘。retry2 根目录 `stage2_data_selection_pca_diff_v1_repair1_matched_budget_434_retry2/` 的 step2000 结果为 Diff：ID `92/100`、Stage2 OOD `45/100`；Internal PCA：ID `91/100`、Stage2 OOD `74/100`。当前不重跑任何训练或评测；Agent 只需完成四份 summary 的视频/actions 交叉验收、reconciliation marker 和结果索引同步。旧 retry1、patience=2 和 timing-only 产物继续保持 diagnostic 边界。
- **OpenDrawer**：v7 从 step4000 checkpoint 继续 ID-only 训练已完成。step6000=`38/100`、step8000=`41/100`、step10000=`34/100`，最终 `100/100` 视频齐全，已写入 `ID_BASE_PROTOCOL_FAILED`。不得进入 OOD/PCA/DAgger；后续只能在同一 ID 分布扩充 demonstration 后重新训练。
- **StackCube Stage1 radial-distance Two-Way**：新任务已认领，旧 diagonal red-position v1/v2 只作 diagnostic。新版本只改变 `red_ood = red_id + 0.04 * normalize(red_id - green)`，保持 green、robot/camera/instruction、其他随机性、success predicate 和 immutable `ckpt-7500` 不变。当前仅做本地 task-spec、paired-reset 和 Oracle runner；在 H20 Oracle 与 20 条真实 `Policy -> Expert -> Policy` continuation smoke 通过前，不进入 PCA/Diff/Failure-Recovery/Offline BC，也不启动 StackPyramid。
- radial 本地前置证据已通过：20 个 paired seeds `971000--971019` 的固定 4 cm radial shift、green/non-target invariants 和 reset predicates 全 false；报告 `docs/experiment_management/diagnostics/stackcube_stage1_radial_two_way/paired_reset_smoke.json`。下一步只做 H20 real-runtime reset/Oracle smoke，失败则停在新 diagnostic 根目录。
- radial H20 real-runtime reset smoke 已通过：新 ID/OOD environment IDs 可实例化，20 个 paired seeds 的 green pose、robot qpos、red-only 4 cm radial shift、metadata 和 reset predicates 全通过；报告位于 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stackcube_stage1_radial_two_way_v1/real_runtime_reset_smoke_retry1.json`。
- radial Oracle smoke 已通过：ID/OOD 各 `20/20` strict success，视频/actions/state/metadata 全部完整，OOD Oracle phase 顺序 reach→grasp→close→lift→align→release；结果位于 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stackcube_stage1_radial_two_way_v1/oracle_smoke_retry1/`。continuation 后台 PID `1167684` 正在 GPU0 做独立 ID calibration，GPU1 保留空闲；在真实 `Policy -> Expert -> Policy` smoke 通过前不启动正式 gated collection/training，也不启动 StackPyramid。
- radial continuation smoke 已收口为 diagnostic：独立 ID calibration 47/50 successful，upper=`1.6427710056`、lower=`1.0735905170`；OOD-only 20 条中 takeover=`14/20`、real return=`11/20`、continuation success=`3/20`、false release=`5/20`。handoff audit 显示 `11/11` returned episodes 的红块在 stable-lift handoff 时仍超出 ID object-green distance `[0.08,0.10]` m，结论为 `RADIAL_CONTINUATION_GATE_FAILED_PERSISTENT_CARRIED_OOD`。持久化结果为 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stackcube_stage1_radial_two_way_v1/continuation_smoke_retry1/`；不启动 PCA/Diff/Failure-Recovery/BC，不启动 StackPyramid。
- **StackPyramid Oracle Repair**：radial Two-Way 已科学封存后，唯一当前 Stack Owner 转入 v4 Oracle repair。先只读审计现有 Oracle actions/state/videos，确认是否存在 red 的提前 release/regrasp/re-transport；确认后只改 Oracle action sequence，保持 v4 geometry、instruction、success predicate、horizon 和 seed semantics。独立 plan 为 `docs/experiment_management/plans/StackPyramid_Oracle_Repair.md`，在5-seed/20-seed/100-seed门通过前不采集新 demos、不训练 ID base、不启动 OOD。
- **StackPyramid formal ID gate**：repaired Oracle、256-ID collection/audit、fresh training 与每1k的20-ID probes均已完成到 `TRAINING_COMPLETE`，但20-probe仅作diagnostic。首个正式100-ID evidence launcher在第0条因 CUDA state tensor serialization 工程错误退出，已写 `FORMAL_GATE_FAILED_ENGINEERING_STATE_SERIALIZATION`，没有正式SR或`ID_BASE_VALIDATED`。最小修复为 state `detach().cpu().numpy()`，源码提交 `bb2a64e`；遵守当前边界不在本阶段再次启动 evaluator，OOD/PCA/timing继续锁定。

### 2026-08-18：下一轮实验的阶段诊断

- **OpenDrawer ID expansion recovery**：首次 Oracle smoke 的 `mplib 0.1.1 + numpy 2.4.4` native SIGSEGV 已定位；`mplib 0.2.1` 与当前 ManiSkill API 不兼容，因此最终采用隔离 `numpy 1.26.4 + 原配 mplib 0.1.1`。planner readiness `1/1`、20条真实 Oracle smoke `20/20` 且HDF5/state/action/video审计通过。新增 collection `128/128`、旧+新数据审计、merged norm和2-step reload/forward smoke均通过；v4/v5路径问题保留 diagnostic。v6已从immutable step4000启动正式ID-only training，当前进入6000门，未进入OOD。
- **StackCube Stage 2 two-way gate**：第一轮 smoke 的 upper/lower threshold 使用了错误的 PCA residual 切片，因而全部未触发，已保留为 diagnostic。retry3 已改为 canonical `coordinates[:, :d-r]` residual，并重新校准得到 upper=`1.9148033`、lower=`1.0915937`；20 条 one-way/two-way smoke 各有 `18/20` takeover、`3/20` success、expert action ratio=`0.5222`，但 two-way return-to-policy=`0`、second takeover=`0`。当前只证明 request-to-expert 已恢复，尚未证明 return-to-policy；Agent 正在审计专家接管后 score 是否自然低于 lower，未启动正式100条评测。
- **StackCube transient-OOD candidate audit**：本地只读盘点已完成，未启动服务器实验。审计结论是单因素 transient OOD 最多能验证一次 return-to-policy，不能可靠产生 second takeover；完整 two-way 需要预注册两窗口组合。首选候选为 StackPyramid Stage1+Stage3，其次为 OpenDrawer Handle+Goal；UncoverSpherePlace Handle+Goal 语义清晰但 Oracle 慢且 ID base 失败。报告为 `docs/experiment_management/StackCube_two_way_transient_candidate_audit.md`，所有现有主结果和 diagnostic 保持不变。
- **OpenDrawer Handle+Goal two-window 本地设计**：已核对现有 Handle/Goal split、paired 参数、Oracle continuation 和 ID recovery 边界。新组合必须使用独立 task-spec/split；当前目标托盘从 reset 可见，可能让 Goal OOD 提前污染第一窗口，必须先做 visibility/state-contamination smoke。报告为 `docs/experiment_management/OpenDrawer_HandleGoal_two_window_asset_seed_state_audit.md`，在 ID recovery 通过前不启动服务器实验。
- **OpenDrawer Handle+Goal two-window GO/NO-GO package**：本地冻结了组合 task-spec 草案、paired seed manifest、reset/intermediate/return/second-window/final-success predicates、one-way/two-way 日志 schema，以及 `tools/audit_open_drawer_handle_goal_visibility.py` 的判定规则。机器可读包为 `docs/experiment_management/diagnostics/open_drawer_handle_goal_two_window/go_no_go_package_v1.json`，当前明确为 `NO_GO_UNTIL_ID_BASE_VALIDATED_AND_COMBINED_ENV_SMOKE`；已完成 JSON/compile/static tests，未连接服务器、未占 GPU。Goal tray 从 reset 可见，若 paired timeline 在 first-window predicate 前出现唯一 OOD upper alarm，则直接判 NO-GO，不得调阈值、改 success predicate 或复用旧失败 base。
- **StackCube Stage 1 red-position**：现有 Stage2 reset 同时写入红块和绿块，不能直接保证只改变红块初始位置，因此已实现独立 Stage1 模块、环境ID、paired sampler、state predicates、测试和 smoke 脚本；本地与H20各20-seed paired-reset smoke均通过（绿块/robot qpos严格相同、红块唯一位移、reset predicates全false、metadata完整）。下一步只跑ID/OOD各20条Oracle smoke，Stage2主结果不变。
- Stage1 Oracle smoke retry2 已通过并完成 reconciliation：ID/OOD 各 `20/20` strict success、`20/20` videos/actions，phase rates、state timeline、failure denominator 均已保存；retry1 renderer partial 保留 diagnostic。task spec/paired manifest 在 smoke 后冻结，下一步仅做 `970100--970199` 各100 Oracle gate，不启动 base gap 或 two-way。
- Stage1 Oracle gate retry1 已通过：ID/OOD 各 `100/100` strict success、`100/100` videos/actions，state timeline、metadata 和 phase counts 完整，runner 按 split 复用单一 gym 环境且无 renderer 崩溃；结果根为 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stackcube_stage1_red_position_ood_v1/oracle_gate_retry1/`，reconciliation 为 `docs/experiment_management/diagnostics/stackcube_stage1_red_position/oracle_gate_reconciliation_retry1.json`。下一步仅准备 immutable base passive ID/OOD gap，two-way 和 training 继续锁定。
- Stage1 passive base gap retry1 已完成：ID `87/100` success、`100/100` ever grasped/lifted、`93/100` on-green；Stage1 OOD `0/100` success、仅 `1/100` ever grasped/lifted、`0/100` on-green。两 split 各 `100/100` videos/actions，state timeline/metadata完整。ID base gate达到 `>=80`，但 OOD prefix/target-stage reach 失败，因此停止在 `BASE_GAP_DIAGNOSTIC_OOD_PREFIX_FAILED`，不启动 two-way 或 downstream training。
- Stage1 red-position v2 fixed diagnostic screen 已完成：ID baseline passive=`18/20`、grasp/lift=`20/20`；v2_scale_040 Oracle/passive/grasp/lift=`18/20,1/20,16/10`；v2_scale_060=`16/20,0/20,11/4`；v2_scale_075=`17/20,0/20,12/3`。没有候选满足预注册规则，写入 `V2_SCREEN_NO_ELIGIBLE_CANDIDATE`，不再追加translation、不启动v2 formal gate、two-way或training。
- **Stage2 intervention metrics evidence integration**：已冻结 `Stage2_intervention_utility_evidence.json` 与分析口径。timing 四条件的 DCA/EAS/DCE 仅做描述性 intervention-quality 指标（n=4，EAS/DCE 与 OOD SR 不呈正向关系）；PCA/Diff 在相同434-action matched budget下的 OOD SR=`74/100` vs `45/100`支持下游学习差异，但不与timing组做 pooled correlation。论文应削弱“指标正向预测 utility”，保留“指标描述专家数据形态、matched-budget方法比较支持 downstream utility”。
- Stage1 real-runtime smoke 已通过。此前桌面SSH权限阻塞仅作diagnostic；主线程补齐了 H20 runtime path 后完成20-seed验证，报告已同步到 `docs/experiment_management/diagnostics/stackcube_stage1_red_position/real_runtime_smoke_retry1.json`。当前尚未运行Oracle或policy实验。

### 2026-08-17：下一轮科学方向

当前结果不能归结为单一的“X-VLA 容量不足”。同一 X-VLA 在 StackCube Stage 2 上经过 matched-budget 更新后达到 PCA OOD `74/100`、Diff OOD `45/100`；而 StackPyramid 的失败集中在 red placement/release，OpenDrawer 的失败集中在抓取、抬升和最终放置。下一轮因此拆成两个互补问题：

1. **数据覆盖与 ID base 能力**：OpenDrawer 在原 ID 分布新增 demonstrations，保持任务语义、Oracle、horizon、norm 和 temporal mask 不变，验证 ID success 是否因覆盖增加而恢复。若仍失败，才进一步归因于长时序闭环或模型适配。
2. **控制权交还机制**：复用已有 StackCube Stage 2 OOD，比较 one-way 和 two-way gate 的 return-to-policy、二次 takeover、专家动作比例、false release 与 task success。该实验验证 two-way gate，不修改已完成的 PCA/Diff 主结果。

公开 ManiSkill 结果也显示，StackCube 性能高度依赖模型、训练数据和 adaptation：公开基线中 OpenVLA、RDT 和 Diffusion Policy 在 StackCube 上的结果差异很大。因此下一轮优先做 data-scaling 和 task-level gate validation，不先盲目更换模型。
- 训练衔接审计发现：Diff formal `train.py` 直接进程不会自行写 `TRAINING_COMPLETE`，若只等待该 marker，PCA 控制器会在训练完成后停滞。已在本地 `tools/xvla_stage2_pca_diff_controller.sh` 增加“进程退出 + ckpt-10000 + 20 个归档 marker”验证逻辑，并在 H20 部署独立 `diff_completion_watcher.sh`（PID `769978`）作为本次运行的旁路收尾；该修复只补完成标记，不改变训练、数据、阈值或评测协议。后续以 retry2 的最终 checkpoint、selection、四份 final summary 和 `PIPELINE_COMPLETE` 为准。

### 2026-08-17：Stage E 最小双向门 smoke 收口

- Stage2 canonical 根目录 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stage2_data_selection_pca_diff_v1_repair1_matched_budget_434_retry2/` 已完成最终 reconciliation；四个 final 分支均为 `100/100` 视频与 actions，主结果未重跑、未修改。
- 由于当前没有可复用的 Stage E 真实任务资产，复用了已审计的 logic/schema smoke，并在 canonical 根目录写入 `stage_e_bidirectional_gate_smoke_reconciliation/`。`summary.json` 与 `SMOKE_COMPLETE` 明确记录：upper threshold=`1.0`、lower threshold=`0.7`、稳定恢复=`2` 次决策、最小 expert dwell=`1`、cooldown=`1`、切换 `4` 次、二次 takeover=`true`、expert action ratio=`0.3`。
- smoke 仅证明双阈值 hysteresis、防抖、dwell/cooldown、二次 takeover 和日志 schema；`recovery_success_rate=null`，没有执行任务级 Stage E SR。`STAGE_E_ASSET_MISSING` 保留该停止原因，Stage2 主结果不受影响。
- Paper-ready final report 已整理为 `docs/experiment_management/StackCube_Stage2_paper_ready_final_report.md`，并同步至 canonical retry2 根目录的 `paper_ready_final_report.md`；报告明确区分 data selection、downstream learning 与不可过度解释的结论。

### 2026-08-18：StackCube Stage 2 task-level two-way gate smoke diagnostic

- 使用同一 Stage2 OOD task、`ckpt-7500` 和 Internal-PCA assets，先按固定 ID manifest `110000--110049` 做独立成功-ID calibration：50 次尝试中 25 条成功、25 条失败；冻结 upper=`16.8112602234`（成功轨迹最大分数的 split-conformal q=0.95）、lower=`14.4281349182`（同一成功集决策分数 q=0.75）、stable low=`2`、dwell=`1`、cooldown=`1`。旧 smoke 的 `1.0/0.7` 未使用。
- 20 条 OOD-only task smoke 使用固定 seeds `140000--140019`，one-way 与 two-way 各有 `20/20` 视频和 actions。两者均为 success `0/20`、takeover `0/20`、expert action ratio `0`；two-way 的 return-to-policy=`0`、second takeover=`0`。因此未证明真实控制权交还和二次接管，未解锁固定 `130000--130099` 的 100 条正式评测。
- 诊断根因是冻结 upper threshold 下 smoke 的 PCA score 没有超过触发线，导致两种 gate 都没有进入专家控制；这不是调整阈值或补跑正式评测的理由。结果根目录为 `/mnt/data/ask4help/results/xvla_stackcube_v1/temporal_mask_v2/stackcube_stage2_two_way_gate_v1/`，包含 `CALIBRATION_COMPLETE`、`SMOKE_FAILED` 和 `DIAGNOSTIC_STOPPED_SMOKE_FAILED`。Stage2 PCA/Diff 主结果与所有旧 diagnostic 目录均未修改。

### 2026-08-18：Stage2 two-way gate score-separation audit

- 首轮 smoke 的无触发已定位为 runner 与 canonical detector 的 PCA residual 切片不一致：canonical collector 使用 `coordinates[:, :d-r]`，首轮 runner 使用了 `coordinates[:, r:]`。只修复该实现差异后，在全新 retry3 重新完成 calibration 和 20 条 smoke；旧 retry1/retry2 目录保留 diagnostic。
- retry3 独立 ID calibration 仍使用固定 `110000--110049`，25 条成功轨迹冻结 upper=`1.9148032665`、lower=`1.0915937424`。two-way 的 287 个专家决策中最低 score=`1.108666`，`0/287` 低于 lower；仅 `9/287`（`3.1%`）落入成功 ID 决策分数 q95 内。Oracle phase 分布为 reach=`27`、grasp=`19`、close=`16`、lift=`98`、align=`97`、release=`30`，各 phase 均无低于 lower 的决策。
- one-way/two-way 各 `20` 条，均为 success=`3/20`、takeover=`18/20`、expert action ratio=`0.5222`；two-way return-to-policy=`0`、second takeover=`0`。同一 seed 的两种 score timeline 长度一致且最大绝对差为 `0.0`，说明没有进入状态机分叉。
- Stage2 改变的是持续存在的 green target position OOD；专家完成 grasp/lift 后目标仍处于 OOD 配置，因此该 task 适合验证 request-to-expert，不适合单独验证自然 return-to-policy。正式 `130000--130099` 评测保持锁定。审计报告为 `docs/experiment_management/StackCube_Stage2_two_way_gate_score_separation_audit.md`，远端 retry3 根目录保留完整 smoke 证据。

### 证据来源

- `docs/experiment_management/共享实验协议.md`
- `docs/experiment_management/训练与持续运行协议.md`
- `docs/experiment_management/plans/StackPyramid_v4冻结任务规范.md`
- `docs/experiment_management/plans/Takeover_Timing_Sweep.md`
- `docs/experiment_management/实验总计划.md`
- `docs/experiment_management/结果索引.md`
- 上级 Scene Workspace 中的当前论文 PDF、8.13 阶段总结 PPT、8.11 Ask for Help PPT

### 2026-08-19：夜间总控 live audit 与 ID gate 口径纠偏

- **OpenDrawer**：没有找到通过 provenance 的 task-specific OpenDrawer X-VLA checkpoint；当前运行明确是已获批准的 π0.5 fallback，不得在论文或结果索引中称为 X-VLA。远端 controller `1643630`、training `1709817` 仍活跃，实际日志已推进到约 `10128/11000`，loss finite、`valid_action_ratio≈0.97`；`global_step_10000` 已落盘。根 `/tmp` 约 95% 满，但 `/sdd` 仍有充足空间；只允许清理本任务已结束临时目录，不得触碰 active Ray session、checkpoint 或其他用户进程。训练完成后必须运行完整独立 `100-ID` gate（100 episodes、视频/actions/summary/provenance齐全），只有 `>=80/100` 才能推进 13000/15000 recovery 或申请 OOD。
- **StackPyramid Oracle Repair**：fresh ID training 使用原始 canonical X-VLA base 与修复后的 256 条 ID 数据。远端训练 PID `1233158` 在唯一 step-9000 probe evaluator `1265445` 运行期间暂停；当前尚无 step-9000 `summary.json`，因此不得恢复训练或启动重复 evaluator。每 checkpoint 的 20 条 probe 仅用于进度/selection 诊断，不能写成正式 ID qualification；训练到 step10000后必须额外执行全新独立 `100-ID` gate，要求 `>=80/100`、100/100 evidence 完整，未通过则 OOD/PCA/timing 全部锁定。旧 20-probe marker 如已写入，须在 reconciliation 中明确其非正式 gate，不得覆盖历史 diagnostic。
- **StackPyramid Oracle Repair**：fresh ID training 使用原始 canonical X-VLA base 与修复后的 256 条 ID 数据。step-9000 probe evaluator `1265445` 已完成，summary 为 `0/20` strict success、`1/20` ever-grasped、`20/20` 视频，controller 已恢复训练至 step10000。该 20 条结果只能作为 probe diagnostic，不能写成正式 ID qualification；训练到 step10000 后必须额外执行全新独立 `100-ID` gate，要求 `>=80/100`、100/100 evidence 完整，未通过则 OOD/PCA/timing 全部锁定。旧 20-probe marker 如已写入，须在 reconciliation 中明确其非正式 gate，不得覆盖历史 diagnostic。
- **StackPyramid controller audit finding**：当前远端 recovery controller 的历史实现仍会在训练结束后仅依据 20-episode probe 写 `ID_BASE_VALIDATED`，且通用 policy evaluator 默认不保存 per-episode action/state files。该实现不满足正式 gate 口径；StackPyramid Agent 已被要求做最小修复：保留20条 probe为diagnostic，新增冻结seed的独立100-ID gate、100/100视频/actions/state/phase evidence审计和 `>=80/100` formal marker。修复验证前不得把任何 `ID_BASE_VALIDATED` 或20-probe marker用于解锁下游。
- **StackPyramid step-10000 phase transition diagnostic**：`TRAINING_COMPLETE` 与 `ckpt-10000` 已存在，但旧 recovery controller 在训练进程自然退出后仍执行 `kill -STOP 1233158`，因 PID 已退出而在 `set -e` 下提前结束；状态停在 `id_gate_step_10000/RUNNING`，没有产生评测 summary，也没有误写正式 gate。Agent 正在从已落盘 checkpoint做最小恢复并修正该衔接逻辑；旧 probe 和半状态均保持 diagnostic，OOD/PCA/timing 继续锁定。
- **StackPyramid formal 100-ID gate recovery**：在确认 H20 无重复 evaluator、GPU0 空闲后，总控从 immutable `ckpt-10000` 启动唯一正式 gate，launcher PID `1269388`、evaluator PID `1269390`，冻结 seeds `84400--84499`。新 evaluator 只通过可选 `--formal-evidence` 增加逐条 actions/state timelines，不改变旧 probe；输出为 `formal_id_gate_100_retry1`，完成后必须同时验证 100/100 视频、actions、state、phase evidence 和 strict success `>=80/100`，再决定是否写正式 marker。
- **StackPyramid formal gate retry**：retry1 在首条 episode前发现 GPU state tensor 未先转 CPU，未产生 episode，保留为工程 diagnostic；已修复为 `json_state(...detach().cpu())`，换全新 `formal_id_gate_100_retry2` 目录重新启动，launcher PID `1270020`、evaluator PID `1270022`，旧 partial 不覆盖、不计入正式分母。
- **StackPyramid formal gate retry2 scientific closure**：正式 gate 已完整收口，`episodes=100`、`video_count=100`、`action_array_count=100`、`state_timeline_count=100`，`formal_evidence_errors=[]`；strict success=`0/100`、ever-grasped=`5/100`。持久化目录为 `/mnt/data/ask4help/results/xvla_stackpyramid_oracle_repair_v1/formal_id_gate_100_retry2/`，marker=`FORMAL_ID_GATE_FAILED` 与 `ID_BASE_NOT_ACCEPTED_FORMAL_100`。该 base 不进入 prefix/locality、OOD/PCA/timing；retry1工程失败与20-probe结果继续单独保留。
### 2026-08-19：OpenDrawer π0.5 fallback 11000 gate与13000 recovery

- X-VLA provenance最终结论仍为无OpenDrawer专用checkpoint；当前主线明确标注为π0.5 fallback，从immutable v6 `global_step_9500`继续，未使用generic X-VLA、v7或其他任务checkpoint。
- 11000 checkpoint的full weights与DCP完整；新evaluator完成独立100-ID gate，`episodes=100`、视频/actions/states/timelines/provenance均为100/100，tail/shape evidence通过，strict success=`3/100`，阶段率为 drawer_opened=`0.74`、grasp=`0.19`、lift=`0.12`、in_target=`0.03`。
- evaluator在全部产物写完后出现`free(): invalid pointer`，已写 `GATE_RECONCILIATION_STEP_11000.json`，明确这是清理异常而非证据缺失；11000保持diagnostic，未写`ID_BASE_VALIDATED`。
- 旧controller已在训练退出前安全停止，训练未中断；新controller PID=`3057205`接管并启动13000 training PID=`3641277`。当前约step=`12084/13000`，step11500 checkpoint已完整，loss finite，`valid_action_ratio`约0.96，Flow-SDE、旧/新256 source balance、frozen norm、temporal mask和success predicate不变。
- 根盘风险只迁移了已结束的本任务`od_recovery_train_11000`到`sdd`，活跃13000 Ray session与其他用户目录未动；资源证据为 `provenance/disk_risk_decision_training_to13000.json`。下一道门是13000完整100-ID gate，失败继续15000，OOD/PCA/DAgger继续锁定。
- **OpenDrawer fresh-base switch**：按Leader决定停止step9500 continuation；controller/training/worker已安全退出，continuation的step11500及11000 diagnostic gate全部保留。immutable π0.5 step4000的full_weights、DCP optimizer state、训练配置、256-ID merged dataset、frozen norm、instruction、temporal-mask/tail-anchor与Oracle/reload evidence已核验。新的fresh launch package位于 `/sdd/ask4help-open-drawer/results/open_drawer_fresh_base_from_step4000_v1/provenance/fresh_base_launch_package_v1.json`，marker=`LEADER_SWITCHED_TO_FRESH_BASE_DIAGNOSTIC`；未启动GPU，等待批准新增256 ID后fresh train。
- **OpenDrawer train-eval distribution audit**：发现训练配置的简化 prompt 与 canonical evaluator 的完整 `TASK_INSTRUCTION` 不一致；三处 pi0.5 YAML 已统一为 `open the drawer, retrieve the blue object, and place it in the green tray`。evaluator 已改为每 episode 独立创建/关闭环境，记录并硬校验实际 `env_id`、control mode、物理 reset 范围、实际 camera key/shape、reset lifecycle、actions/states/timeline/tail evidence；CUDA tensor 状态统一经 `detach().cpu().numpy()` 转换。远端 focused tests 为 `7 passed`；全新 protocol smoke `3/3` episodes、视频/actions/states/timelines/reset metadata 均完整，reset 范围与 lifecycle 校验通过，strict success=`0/3`仅作 smoke 结果。输出为 `/sdd/ask4help-open-drawer/results/open_drawer_fresh_base_from_step4000_v1/protocol_smoke_v2/`，marker=`PROTOCOL_SMOKE_PASSED`；`free(): invalid pointer` 已按 `SIMULATOR_EXIT_AFTER_ARTIFACTS` reconciliation。旧11000 strict=`3/100`因简化训练 prompt 与 canonical evaluator 不一致，明确为 `INVALID_COMPARISON_PROMPT_MISMATCH_DIAGNOSTIC`，不解锁训练或 OOD。

- **2026-08-22 OpenDrawer 主线切换**：用户明确要求停止当前 X-VLA v8 训练并切回 π0.5，从历史 `global_step_10000` 继续。X-VLA v8 在完整 `ckpt-35000` 收口，写入 `USER_SWITCHED_TO_PI05_DIAGNOSTIC`，不删除、不覆盖、不进入 selection/formal/OOD。π0.5 源 checkpoint、full weights、DCP、128-ID 数据、冻结 norm、完整 prompt、Flow-SDE、train_expert_only、AWBC=false、global batch 128/micro32、action_valid_mask 已审计。新 pipeline 为 `open_drawer_pi05_resume_from10000_v1`，先 `10000→10002` smoke，再总 step `20000`，每500保存并核对，OOD保持锁定。

### 2026-08-22：PickSingleYCB object-variation pipeline 认领

- 当前新实验唯一 Owner 为 `codex-object-variation-pick-single-ycb`，thread=`019ffbc4-f3a9-78f3-8684-e0b4cba3552a`，run root=`/data/zhaozhixuan/Ask4Help-airplane-5090/results/object_variation_pick_single_ycb_v1/`。
- 任务冻结为 generic PickSingleYCB pick-and-place：ID 使用 `005_tomato_soup_can`，OOD 使用 held-out `008_pudding_box`；只改变 `object_model_id`，位置、姿态、目标、机器人、相机、语言、success predicate 和控制协议保持配对。
- 5090 通过实时 preflight 选择；GPU2 的已有 PID `1198612` 不可碰，暂定只使用实时确认空闲的 GPU0/1/3/4。H20 因已有 Ray/online-awbc owner 和 root filesystem 满载被拒绝。
- manifest=`configs/pipelines/pick_single_ycb_object_variation_pi05_v1.json`；task plan=`docs/experiment_management/plans/PickSingleYCB_ObjectVariation_OOD.md`；当前 stage=`id_sft_formal_training_retry1`，下一阶段=`id_checkpoint_probe_and_early_stop`。Oracle gate、ID data audit、ID norm 与 2-step reload/forward smoke 已通过；ID formal base training 正在 GPU0 单卡重试，ID gate 前不解锁 OOD。
- 5090 首次 smoke 发现共享 NumPy 2.4.4 下官方 `mplib.Planner` 构造 SIGSEGV；ManiSkill/MPlib 上游已有同类 planner 静默退出问题。未修改共享 venv，新增 pipeline-only NumPy 1.26.4 overlay；修复后 ID/OOD 各1条真实抓取、抬升和放置 smoke 均通过。首版 smoke 的 reset metadata 在完成后读取，已保留 diagnostic 并修复为 reset 后立即冻结 metadata，正式 gate 使用修复版。

### 2026-08-23：PickSingleYCB object-variation ID checkpoint early-stop decision

- 用户明确改变 ID 训练阶段决策：每个完整的 `global_step_500` 间隔 checkpoint 都在独立20条 ID probe 上评测；严格成功率大于80%（整数实现为至少 `17/20`）时立即优雅停止 ID SFT，并将第一个通过的 checkpoint 作为正式 ID checkpoint。
- 独立100-ID评测仍保留为 formal checkpoint validation evidence，但不再阻止已经通过 `17/20` probe 的 checkpoint 解锁下游；若直到10000步没有 probe 通过，则回退到全训练后的 checkpoint selection 与100-ID gate。所有 probe 视频、summary、actions 和选择记录必须保留。
- post-training controller 已更新为阶段感知的 `id_checkpoint_probe_and_early_stop`，从已落盘 checkpoint 继续，不覆盖已有训练或 diagnostic 产物；manifest、task plan、ACTIVE_PIPELINES 和 Heartbeat Watchdog 已同步该决策。
